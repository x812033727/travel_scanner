# 工作交接

## 狀態
分支：填入實際分支；提交：填入 git rev-parse HEAD 的結果。
已完成：待辦新增／刪除與核心測試。
尚未完成：全部、未完成、已完成的篩選介面。

## 已確認事實
model.js 以 id 操作；index.html 是畫面入口。

## 下一步
先執行 node --test tests/model.test.mjs，再新增只讀的 filterTodos 函式。

## 未驗證
這份範本尚未記入你的實際測試結果；不要直接寫成全部通過。
