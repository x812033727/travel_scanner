import { Check } from "lucide-react";
import { SiteHeader } from "@/components/site-header";

const plans = [{ name: "FREE", price: "NT$0", credits: "每月 20 credits", items: ["分開搜尋各旅遊模組", "儲存 1 份旅程", "1 個價格通知", "每月 1 次完整最佳化"] }, { name: "PRO", price: "NT$299", credits: "每月 200 credits", items: ["完整旅程與彈性日期", "多城市與附近機場", "三種最佳化模式", "20 份旅程與 20 個通知"] }];
export default function PricingPage() { return <><SiteHeader /><main className="mx-auto max-w-4xl px-5 py-14"><div className="text-center"><p className="text-sm font-semibold text-[var(--teal)]">不做假的 Unlimited</p><h1 className="mt-2 text-4xl font-bold">清楚、可稽核的搜尋點數</h1><p className="mt-3 text-[var(--muted)]">每一筆搜尋成本都能查核，PRO 正式訂閱付款尚未開放。</p></div><div className="mt-10 grid gap-5 md:grid-cols-2">{plans.map((plan) => <article key={plan.name} className={`rounded-[2rem] border bg-white p-8 ${plan.name === "PRO" ? "border-[var(--teal)]" : "border-[var(--line)]"}`}><p className="font-semibold text-[var(--teal)]">{plan.name}</p><h2 className="mt-3 text-4xl font-bold">{plan.price}<span className="text-sm font-normal text-[var(--muted)]"> / 月</span></h2><p className="mt-2 text-sm text-[var(--muted)]">{plan.credits}</p><ul className="my-7 space-y-3">{plan.items.map((item) => <li key={item} className="flex gap-2"><Check size={18} className="text-[var(--teal)]" />{item}</li>)}</ul><button disabled className="w-full rounded-xl bg-[#e4ebe6] p-3 font-semibold text-[var(--muted)]">{plan.name === "FREE" ? "註冊後自動啟用" : "即將開放"}</button></article>)}</div></main></>; }

