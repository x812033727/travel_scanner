/** Labels only: importing this module never loads a lesson catalogue. */
import messages from "@/messages/zh-TW/common.json";

export const geminiSeriesCopy = messages.geminiSeries;
export type GeminiSeriesCopy = typeof messages.geminiSeries;
