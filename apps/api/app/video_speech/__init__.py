"""Narration for the local YouTube video pipeline (tools/video), synthesized on this server.

The Azure Speech key lives in the encrypted provider settings and never leaves the server: the
pipeline sends sentences with a video tool token and gets audio back. See docs/videos/DESIGN.md.
"""
