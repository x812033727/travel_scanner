interface:
  display_name: "Dev and CI checks"
  short_description: "Run local checks on Windows, triage CI and Dependabot"
  default_prompt: "Use $dev-and-ci to run the checks my change touches and explain any red CI."
