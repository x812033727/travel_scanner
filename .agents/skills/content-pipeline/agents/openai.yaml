interface:
  display_name: "Content pipeline"
  short_description: "Write, verify, import and publish article batches"
  default_prompt: "Use $content-pipeline to run the next article batch from its spec to production."
