interface:
  display_name: "Production deploy"
  short_description: "Preflight, deploy and verify the production host"
  default_prompt: "Use $deploy to check the production host and deploy the latest main."
