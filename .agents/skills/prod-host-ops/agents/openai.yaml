interface:
  display_name: "Production host operations"
  short_description: "Diagnose, change and verify the live site between deploys"
  default_prompt: "Use $prod-host-ops to find out why the production site is returning 429s and prove the fix."
