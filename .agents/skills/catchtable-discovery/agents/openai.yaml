interface:
  display_name: "CatchTable discovery"
  short_description: "Find Korean merchants from CatchTable rankings and fill reservation links"
  default_prompt: "Use $catchtable-discovery to run the next CatchTable ranking batch from collection to the importer files."
