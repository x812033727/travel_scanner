interface:
  display_name: "Backend conventions"
  short_description: "Migrations, caller-session helpers and new admin pages or settings"
  default_prompt: "Use $backend-conventions before writing an Alembic migration or adding an admin page."
