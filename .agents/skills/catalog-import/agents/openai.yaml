interface:
  display_name: "Catalog data CLIs"
  short_description: "Run the host-side data CLIs for merchants, hotspots, holidays and guide upkeep"
  default_prompt: "Use $catalog-import to import the next merchant batch and apply its reservation links on the host."
