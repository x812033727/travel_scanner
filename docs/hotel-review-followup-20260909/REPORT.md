# 飯店補證審核結果

完成日期：2026-09-09（台灣時間）；本批 `hotel-review-followup-20260909`。

使用內建瀏覽器重新核對 54 筆既有待審連結，正式新增核准 **49 筆訂房連結**。
另 5 筆連結與 13 間取得新補查結果的飯店，寫入保留待審的稽核紀錄。
本批共 67 筆新審核收據；不是把所有資料一律核准，也沒有重複審核沒有新證據的舊項目。

## 正式資料結果

| 類型 | 本輪前已核准 | 本輪新增核准 | 目前已核准 | 目前待補證 |
| --- | ---: | ---: | ---: | ---: |
| 飯店本體 | 40 | 0 | 40 | 20 |
| 訂房連結 | 206 | 49 | 255 | 105 |

新增連結：京都 27、首爾 18、釜山 4。東京、大阪、台北沒有新增核准。
所有 60 間飯店的名稱、來源、座標、地圖身分與狀態完全不變。

前台仍有 40 間公開飯店，公開訂房連結由 165 增至 **183**（新增首爾 18 筆）。
本輪其餘 31 筆核准連結因所屬京都／釜山飯店仍待審而不公開。
目前共 72 筆已核准連結位於待審飯店下；京都、釜山各 0 間公開飯店，但城市開關保持開放。

## 新證據與仍未通過的原因

- 49 筆核准都在內建瀏覽器實際看到相符館名與地址／官方郵寄形式。
  Granvia、Royal Park 與 Westin 部分頁面只提供街道／郵寄形式，不冒稱看見未顯示的門牌。
  正常後端檢查中 15 筆為 `healthy`、34 筆為 `unconfirmed` 且有實際瀏覽器身分證據；
  沒有 `unsafe` 或 `unavailable` 連結被放行，也沒有任何 guard fallback。
- 3 筆 Hyatt 官網（台北君悅、首爾柏悅、釜山柏悅）本次仍呈空白頁；
  2 筆 Trip.com（首爾明洞西鐵、釜山 Stanford）導向登入頁。這 5 筆維持待審，未繞過登入。
- 京都 Granvia、Miyako Hachijo、Westin Miyako 的三個非 OTA、CC0 座標 claim 已核對修訂、
  來源與授權；兩個官方 Place ID Finder 在內建瀏覽器都未呈現可用輸入欄，無法取得精確身分。
  三館保留待審，沒有把新座標寫入正式資料，也沒有推算 Place ID。
- 釜山 10 館補查新的韓國政府住宿許可資料，目錄標示 EPSG:5174 與利用許可無限制，
  但實際來源檔入口回傳 403，內建瀏覽器亦確認受阻。沒有原始匹配紀錄，故沒有猜測或轉換座標。
- 其他既有缺口原樣保留：94 筆沒有精確飯店網址；京都 Hyatt Regency 的 5 筆連結及飯店本體
  因公告預定 2027-05-09 結束營運、現有流程缺少日期防護而待審（不是宣稱目前已停業）；
  1 筆 Daiwa Roynet Agoda 已知安全限制未繞過。其他京都地點仍需來源／地圖條件。

沒有把「尚未確認」改成「平台未販售」。未使用 Gemini 或付費 API，沒有報價、訂房、導購點擊、
額度調高、設定變更、啟用分潤或部署。

## 驗證與回復能力

- 先做新 PostgreSQL 備份：13,082,089 bytes，權限 600、資料夾 700，`pg_restore -l` 可讀；
  未宣稱已做完整還原演練。備份資訊在 `preflight.json`。
- 線上 7 個相關程式檔 SHA-256 與測試版本相符；67 筆 dry-run 成功。
- 63 項離線回歸測試、獨立程式安全審查及 Ruff 通過。
- 全 420 列核對：僅 49 筆平台列變更各 7 個允許欄位，版本各 +1；其餘 **371 列原樣保留**，
  包含全 60 間飯店、原 246 筆核准項目及所有保留／未選項目。URL、provider、property ID 不變。
- 67 筆收據的操作者一致、目標無重複；正常 `hotel_option_review` 稽核 +49，
  `product_edit` 與 `product_review` 都 +0。重播 67 筆全部回傳同一收據及列雜湊。
- 三次獨立 READ ONLY／REPEATABLE READ SQL 快照；重播前後 81 筆結果除快照時間外一致。
  供應商設定、城市設定、分潤品牌、offer、點擊及前批完整 290 筆收據全部不變。
- 前後各五語 × 六城市的 30 次匿名公開 GET 全部 200、`no-store`；逐筆公開身分、來源、
  語系名稱／原名回退與核准父子關係相符。價格、幣別與價格查核時間皆明確為 null，沒有報價。
- 正式 `/ready` 正常，schema 保持 `0063_destination_offers`。

證據入口：`browser-identities.json`、`manifest.json`、`apply.jsonl`、`replay.jsonl`、
`verification-report.json`、`sql-verification.json`、`public-before.json` 與 `public-after.json`。
`remaining-items.json` 列出全部 125 個待補證項目、原因與最後查核批次／時間；舊證據清楚標示，沒有冒稱本輪重查。
前批 `docs/hotel-review-all-20260908/` 完全保留。本次沒有 GitHub push、PR、合併或程式部署。
