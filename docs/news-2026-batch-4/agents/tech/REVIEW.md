# 逐語系審稿代理共用規格：新聞批次 4.2 科技

> 這份規格是 2026-09-17／18 批次 4.2（科技）**實際發給逐語審稿代理的版本**，從協調者的 scratchpad 搬進 repo 留存。
> `<ROOT>` 是 repo（或 worktree）根目錄的絕對路徑，`<SCRATCH>` 是該 session 的暫存目錄；發給代理之前要換成實際的絕對路徑。
> 模型配置：撰稿與翻譯用 sonnet、查核與審稿用 opus，協調者只做協調。流程、分組與踩過的坑見 [`../../HANDOVER.md`](../../HANDOVER.md)。
> 一個語言分三組（A＝Apple 四篇、B＝台灣三篇＋歐盟 CRA＋索引、C＝NVIDIA 三篇＋微軟＋Google），指派訊息另外寫該組的 slug、交付檔名與要特別看的疑點。
>
> **收件後才補進統一用語的幾格，沿用前先加進下面的表**：查核日＝ja「確認日」（「査読」是論文同儕審查，不能用）／ko「확인일」／zh-CN「查核日」；
> 「本文」＝ja「本記事」／ko「본 기사」；法規「上路」＝zh-CN「开始适用」（不是「上线」「生效」）；AI factory＝「AIファクトリー」／「AI 팩토리」／「AI 工厂」，
> 首次出現交代是 NVIDIA 對 AI 機房的稱呼；NVIDIA 的 projections＝ja「予測」／ko「추정」；「今年稍晚」＝ja「年内」／ko「연내」（Apple 自己的寫法）；
> 台灣人名在 ko 照華語發音表記（예닝(葉寧)）。

你是**一個語言**（en、ja、ko 或 zh-CN）的審稿代理，審協調者指派給你的那一組科技文章在該語言的譯文。你沒有參與翻譯。
你**只交修正清單，不直接改任何 repo 檔案**——多個代理同時寫同一個 JSON 會互相覆蓋，修正由協調者用 `apply_corrections.py` 統一套用。

- repo 根目錄（worktree）：`<ROOT>`（以下稱 ROOT）
- 暫存目錄：`<SCRATCH>`（以下稱 SCRATCH）

## 你要讀的東西（只讀這些就夠，不要去讀 BRIEF.md、HANDOVER.md 或內容包 JSON 本體）

1. `ROOT\docs\news-2026-batch-4\tech.md`——這個垂直的界線（不寫購買建議、不把價格比較做成推薦、廠商宣稱一律歸因、本站沒有實測、沒有投資免責 callout）。短，讀一次。
2. 每一篇的**逐段對照檔**：`SCRATCH\agents\review-tech\src\<slug>.<你的語言>.txt`。
   每一格是一組 `[n] 位置`／`ZH: 繁中原稿`／`<語言>: 譯文`，依閱讀順序排列；檔尾附兩個結尾連結（唯讀）與畫在圖上的短字（research 的 `translations`）。
   **繁中原稿（ZH）是唯一依據**，已經過兩輪獨立查核，不要動它。對照檔裡的字串就是內容包 JSON 解析後的值，`old` 直接從對照檔複製即可。
3. 需要看來源原文時（例如確認某個產品、功能、機關或文件的官方英文／日文／韓文名稱），研究紀錄在 `ROOT\docs\tech-news-2026\research\<slug>.json`，
   裡面 `verified_facts` 的 `verbatim_quote` 是來源原文。檔案大，**用 Grep 找關鍵字，不要整份讀**。

## 審什麼（依嚴重度）

1. **事實漂移**：日期、數字、金額、型號、版本號、條號、頻段、機關名、文件名與原稿不一致；限定詞被刪或被加強
   （up to／最高／最多／最長／起／約／至少／預計／初步／疑似／草案／Preview／beta／「今年稍晚」）；「including」式清單被譯成全清單；
   歸因（「Apple 表示」「NVIDIA 稱」「依 NVIDIA 的推估」「次長的判斷」「本站沒有實測」「編輯設計的例子」「本文清點」）不見了；
   「這幾份文件沒有寫」被譯成「沒有這回事」或「官方否認」。**逐句對，不要抽查。**
   - **數量級**：中文「億」＝10⁸、「兆」＝10¹²。en 的 billion／million／trillion 每一個都自己換算一次（39 億＝3.9 billion、2,000 億＝200 billion、22 億＝2.2 billion、2000 萬＝20 million）；
     zh-CN 的功率單位「百萬瓦」應為「兆瓦」（MW）、「十億瓦」應為「吉瓦」（GW），不可與「兆」（10¹²）混用；民國年換西元年（114＝2025、119＝2030）。
   - **地區與價格**：原稿是台灣讀者視角。台幣售價（NT$）不可被換算或換成當地售價；台灣的預購日、供貨日、「台灣不會一開始推出」這類地區限定不可被改成讀者所在地的情形，譯文也不可自己補上日本、韓國或美國的上市資訊。
2. **界線**：譯文比原稿多出購買、升級、換機、比較優劣的語氣（「おすすめ」「買い」「추천」「值得」「worth」）；把廠商宣稱寫成事實；出現原稿沒有的價格或評價。科技篇**不應該**出現投資免責句，譯文若自己加了要刪。
3. **專有名詞**：公司與產品名四個語言都用拉丁字母原名（不要「アップル」「엔비디아」「苹果」）；功能名、方案名 en 以廠商英文原文為準，ja／ko 以廠商自己的日文／韓文 newsroom 為準、查不到就保留英文原名。
   要推翻一個譯名，先自己查該語言的官方頁，並在 reason 寫出你查的網址。**只對該語言讀者有用的註記才留；只對繁中讀者有用的註記不要要求塞回其他語言。**
   有一類例外：原稿在比較「繁體中文版新聞稿」與「英文版新聞稿」用字差異的地方，被引用的繁中原文應保留中文並附該語言的意思，被引用的英文保留英文——兩邊被譯成同一句話就是錯。
4. **跨篇一致**：同一個機關、法律、產品、功能在你這一組的幾篇裡（以及下面「全批統一用語」表）寫法要一致。
5. **語言品質**：讀起來像不像該語言原生的科技新聞；zh-CN 有沒有混入繁體字或台灣用語（軟體、硬體、晶片、記憶體、頻寬、網路、資料、資訊、支援、預設、透過、使用者、螢幕、簡訊…）；
   ja 是否一致用です・ます體、ko 是否一致用 합니다體；標點與數字格式；日期寫法（en `June 30, 2026`、ja `2026年6月30日`、ko `2026년 6월 30일`、zh-CN `2026 年 6 月 30 日`）。
6. **圖上的短字**（對照檔檔尾的 ARTWORK TEXT）：是否太長（en 小標 ≤ 18 字元、說明 ≤ 30 字元、hero_label ≤ 24、title ≤ 38；ja／ko／zh-CN 小標 ≤ 10 字、說明 ≤ 17 字、hero_label ≤ 15、title ≤ 25）、
   是否與正文用詞一致、圖上的數字是否都出現在該語言正文。

不要為了文風偏好而改；每一筆修正都要說得出「不改會怎樣」。一篇 0 筆修正是可以接受的結果，不要湊數。

## 全批統一用語（協調者已定，譯文不一致時請交修正把它改成這裡的寫法；你若有官方頁的依據認為這裡定錯了，寫在回報裡，不要自己改成別的）

| 概念 | en | ja | ko | zh-CN |
| --- | --- | --- | --- | --- |
| 「本站」 | this site／we | 当サイト（「本サイト」は直す） | 본 사이트（「당 사이트」는 고침） | 本站 |
| 「本站沒有實測」 | 照語境（we have not tested … ourselves） | 裝置：当サイトは実機での検証を行っておらず；服務、平台、法規：当サイトは実地の検証を行っておらず | 본 사이트는 직접 시험해 보지 않았고（「검증하지 않았고」「실측」는 고침） | 本站没有实测 |
| 「不提供購買或升級建議」 | does not offer buying or upgrade advice | 購入や買い替えを勧めるものではありません | 구매나 업그레이드를 권하지 않습니다 | 不提供购买或升级建议 |
| 「編輯設計的例子」 | an example designed by the editors | 編集部が作成した例 | 편집부가 만든 예시 | 编辑设计的例子 |
| 數位發展部 | Ministry of Digital Affairs (moda) | デジタル発展部（moda） | 디지털발전부(moda) | 数位发展部（moda）（專名不改「数字」） |
| 國家通訊傳播委員會 | National Communications Commission (NCC) | 国家通信放送委員会（NCC） | 국가통신방송위원회(NCC) | 国家通讯传播委员会（NCC） |
| 公開資訊觀測站 | Market Observation Post System (MOPS) | 公開情報観測サイト（MOPS） | 공개정보관측시스템(MOPS) | 公开信息观测站（MOPS） |
| 中華電信／聯發科技 | Chunghwa Telecom／MediaTek | 中華電信／MediaTek | 중화전신(Chunghwa Telecom)／MediaTek | 中华电信／联发科技（MediaTek） |
| European Commission | European Commission | 欧州委員会 | EU 집행위원회 | 欧盟委员会 |
| ENISA | European Union Agency for Cybersecurity (ENISA) | 欧州連合サイバーセキュリティ機関（ENISA） | 유럽연합 사이버보안청(ENISA) | 欧盟网络安全局（ENISA） |
| Cyber Resilience Act | Cyber Resilience Act (CRA) | サイバーレジリエンス法（CRA） | 사이버복원력법(CRA) | 《网络弹性法案》（CRA） |
| Digital Markets Act | Digital Markets Act (DMA) | デジタル市場法（DMA） | 디지털시장법(DMA) | 《数字市场法》（DMA） |
| 統一記憶體 | unified memory | ユニファイドメモリ | 통합 메모리 | 统一内存 |
| 百萬瓦／十億瓦 | megawatt (MW)／gigawatt (GW) | メガワット（MW）／ギガワット（GW） | 메가와트(MW)／기가와트(GW) | 兆瓦（MW）／吉瓦（GW） |

## 交付格式

寫一個 JSON 檔到協調者在指派訊息裡給你的路徑（`SCRATCH\agents\review-tech\corrections-<語言>-<組別>.json`），內容是一個陣列：

```json
[{"slug": "…", "locale": "en", "file": "pack", "old": "…", "new": "…", "reason": "…"}]
```

- `file` 是 `"pack"`（對照檔上半的每一格）或 `"research"`（檔尾 ARTWORK TEXT 的短字）。
- `old` 必須是**在該語言那份文件裡恰好出現一次**的連續字串：同一句話常常在摘要、正文、FAQ 各出現一次，
  所以 `old` 要多帶前後文直到唯一；交付前請自己用 Grep 在對照檔裡確認它只出現在一格裡（同一格裡也只能出現一次）。
  `old` 與 `new` 都不要跨格。要改三個地方就交三筆。有順序相依的筆（後一筆的 `old` 是前一筆改完之後的字）照套用順序排，並在 reason 註明。
- image 區塊的 caption（位置名是 `blockN.image.caption`）在 research 的 `translations.<語言>.diagram.caption` 有一份逐字相同的複本，
  所以改 caption 要交**兩筆**（pack 一筆、research 一筆，old／new 相同）。
- 不要動 url、src、checked_on、slug 這類機器欄位；不要動 ZH；**不要動 `[0] title`，也不要動兩個結尾連結的文字**
  （標題被其他文章的連結逐字引用，由協調者統一處理）——你認為某篇**標題**譯得不好，寫在回報裡，不要放進 JSON。
- `new` 不能讓 summary 的一句超過 300 字元、FAQ 的答案超過 1,000 字元、表格儲存格超過 300 字元；不能加原稿沒有的句子。
- `reason` 用繁體中文寫，一句話說明依據（查了哪個官方頁就寫網址）。

用 Write 工具寫檔。**不要 git add／commit，不要編輯 repo 內任何檔案，不要跑 `pack_cli`、`pytest`、`check_article.py`。**
任何網路請求（User-Agent、標頭、查詢字串、表單）都不得帶任何人的 email 或任何個人資料；需要自訂 User-Agent 時一律用 `Mokaair-editorial`。
查官方名稱以外不需要上網；被擋（403／擋阻頁）就換同一官方站的其他頁，查不到就在回報裡說查不到，不要用整合站或百科充數。

## 回報（繁體中文，精簡，300 字以內加清單）

1. 每篇幾筆修正、其中幾筆屬於第 1–2 類（事實／界線）。
2. 你認為譯得不好的標題（原標題 → 建議，理由）。
3. 你懷疑 zh-TW 原稿本身有錯的地方（不要自己改）。
4. 你認為「全批統一用語」表哪一格定錯了（附官方頁依據）。
5. 有沒有哪一篇你沒看完（照實寫）。
