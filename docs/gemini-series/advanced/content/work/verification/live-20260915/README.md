# 2026-09-15 Google Pro 網頁實測

本目錄保存連接使用者已登入 Chrome 後，以虛構教學資料取得的真實輸出。
原作者交付、內容包及歷史驗收收據保留原樣；新增證據不自動解除整套發布門檻。

## 環境與成本

- Windows、Chrome、個人 Google 帳號；網頁顯示 PRO。51／52 文字對話選 Flash；53 在前兩輪 Flash 失敗後切換 Pro 完成後兩輪。
- Flash 是畫面名稱，介面未提供可核對的精確模型 ID，不能當成 Gemini API 型號。
- 第 54 篇使用 Gmail／Docs 的 Gemini 側邊欄，沒有可核對的後端版本；完成合成資料貼入流程，沒有讀取真實郵件。
- 使用者授權既有 Pro 額度；另允許 API，但總額上限 NT$0，僅可確認免費的額度。
- 本輪目前沒有 API 呼叫或額外購買。Process／User／Machine 均未設定 GEMINI_API_KEY 或 GOOGLE_API_KEY；只檢查有無值，沒有讀出或保存金鑰。
- 既有個人化設定保留；沒有獨立控制個人化，因此結果只代表這個帳號、模式及時間的一次測試。
- 只傳送教材的虛構資料。公開追蹤檔不保存帳號姓名、email、舊對話或可定位私人對話的 URL。

## 第 51 篇

`51/environment.json` 記錄凍結輸入及限制。`51/audit-live.py` 核對題庫、提示詞、交替順序與不同對話，不替模型評分。已收集 20 筆；`51/captured.json` 與 `51/raw/` 保存畫面回答本文，這是呈現後的文字，並非 HTTP 回應 JSON 或原始 Markdown。

人工評分是獨立門檻；Codex 的語意初查必須標為 AI 初查，不能填成已完成人工評分。即使 20 筆回答全部存在，也不能據此宣稱改寫版勝出或整篇通過語意驗收。

`51/transport-check.json` 的 20 個 checksum 與瀏覽器保留紀錄一致；用來排除轉寫錯誤，不是第三方真實性認證。`51/capture-audit.json` 記錄輸入及原始檔 SHA256；154 個歷史作者交付檔案雜湊維持不變。

兩份 C08 回答含表格儲存格中的換行與行尾 tab，刻意保留原樣；`.gitattributes` 限定對第 51／52 篇的原始回答 `raw/*.txt` 關閉換行轉換與行尾空白告警。其他文件仍接受一般空白檢查。

## 第 52 篇

使用者回覆「好了」後，同意視窗已消失。已建立虛構客服 Gem、上傳兩版手冊並驗證儲存／重開與來源全文；2 份預覽、v1／v2 各 12 份獨立回答已保存。發現沒有工具卻聲稱已轉交的問題後，另加入一段能力界線並完整重跑 12 題，共 36 份回歸回答。原始與修正版分開存放，沒有重新生成挑答案。

修正版 11 題明確說無法代為轉交，但 G02 無理由拒答、G01／G03 重複文字、G08 的內容物措辭不精確，仍未通過完整品質驗收。詳見 [原始問題](52/baseline-review.md) 與 [修正版逐題初查](52/corrected-review.md)。AI 初查不等於人工核准或整套可發布。

[環境紀錄](52/environment.json) 記錄輸入雜湊、模型名稱與成本；[原始完整性檢查](52/capture-audit.json) 及 [修正版檢查](52/corrected/capture-audit.json) 核對問題、儲存設定、來源與不同對話。36 筆匯出與瀏覽器紀錄的 checksum 全部一致，154 個歷史作者檔案未變。檢查會保存缺少引用的失敗，不能把腳本退出成功當成語意通過。

本篇尚須把真實案例與能力界線補充整合到新版文章與內容包，再建立新的驗收收據。現有作者原稿、正式內容包及發行候選包保持原樣。

## 仍待完成

第 51 篇人工評分；第 52 篇品質處理與語意核准；第 52–54 篇新版 pack 已整合實測、待共享 release 採用；第 54 篇真實郵件擷取及接收者存取不在已驗範圍；第 55 篇實體手機；第 56 篇自然觸發及完整週期停止觀察。共享系列整合、發布、公開頁與 sitemap 驗證仍未完成。這個目錄是增量實測證據，不是整套發布許可。

## 第 53 篇

已完成四輪 Canvas 真實生成與修正。Flash 兩次留下不完整 HTML；Pro v3 可操作，但實際發現大金額差 1 分、錯誤提示缺少關聯、200% 根字級副本溢出。v4 修正後，360／1440px 的 12 筆數值、17 筆非法輸入及 6 筆介面觀察，共 35 項通過。失敗原碼及排除的工具異常均保留，未以作者參考 HTML 替代模型產物。

[開啟成品 HTML](53/budget-v4.html) · [實測與修正報告](53/README.md) · [稽核收據](53/capture-audit.json)。35 筆匯出校驗一致，154 個歷史作者檔案未變。原生瀏覽器放大、實體手機、螢幕閱讀器使用者測試及完整執行期網路追蹤未完成；通過範圍限於明列案例。正式文章與內容包仍待整合，整套尚未發布。

## 第 54 篇

已完成 Gmail 側邊欄貼入五封合成信、建立 Docs／Sheets、Docs 修正預覽並接受、重新開啟與試算表篩選，共四次 Gemini 生成、兩份雲端成品。Docs 首版出現 `Person` 人員佔位晶片，修正為純文字「尚未指派」後重開仍保存。Sheets 四筆待辦的七段引用均能在原文找到；B5 值與公式列空白，待確認狀態篩選顯示 2／4，重設及重載後資料一致。

[實測與修正報告](54/README.md) · [稽核收據](54/capture-audit.json)。19 個證據檔、12 個瀏覽器至檔案校驗及 154 個歷史作者雜湊通過。三個故障注入被拒絕，另保存一個引用雖存在、語意卻錯誤的漏檢案例。沒有讀取真實收件匣、寄信或更改分享，兩份成品只供本人存取；API 呼叫 0、額外花費 NT$0。實際郵件擷取、接收者帳號存取、人工語意核准與文章整合仍未通過，整套未發布。

## 第 52–54 篇新版文章交付

[新版原稿、內容包與下載](../../revisions/20260915/README.md) 已把上述真實案例整合成三篇完整文章，分別 2,556／2,420／2,612 字；保留原章節定位及六張既有配圖，另產出三份實測練習 ZIP 與新收據。各節先前的「文章待整合」是當時狀態，現已完成新版作者交付，接續只需共享 release 審查與重建候選，以及各篇明列的未驗範圍。

三篇 schema、字數、pack lint、資產、站內連結檢查通過；三份 ZIP 解壓後可核對 50／35／28 個來源檔。原 154 個作者檔案與歷史收據未改；正式內容包、網站與發布狀態保持不變。

## 第 55–56 篇新增實測

[第 55 篇照片實測](55/README.md) 保存同一張 CC0 圖片的兩輪 Pro 回答。首輪把倒影清晰度、零件中空與深色物質身分說得太確定，修正輪處理三項疑點，但功能性名稱等推論仍未完全排除。重開後兩份回答 checksum 一致。這是桌面照片測試，Android／iPhone 的相機、Live、停止分享、逐字稿及電腦接續仍待 [實體裝置回報](55/device-checklist.md)。

[第 56 篇 Spark 實測](56/README.md) 完成手動官方摘要、建立每週二 12:15 的真實測試排程、暫停／恢復／再暫停。直到台北 12:24 左右仍未見自然觸發，原因與伺服器時區未確認，沒有點立即執行代替。使用者確認刪除後，12:26 重開排程清單為空，手動摘要原文保留。未等待完整一週，不能宣稱自然觸發及原定下一次停止觀察通過。

三篇摘要的官方日期、連結與主要內容可追溯，但漏掉部分逐步推出與手機限制；模型自報字數亦不符 88／80／71 個字元的實際計數。[來源初查](56/source-review.md) 保留缺失，不把有引用等同品質全部通過。[新增稽核收據](mobile-spark-audit.json) 核對 18 個證據檔、三份回答校驗碼與 154 個歷史檔案，通過範圍限完整性；本輪 API 呼叫 0、額外花費 NT$0，整套尚未發布。
