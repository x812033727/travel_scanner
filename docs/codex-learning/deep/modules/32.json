{
  "id": 32,
  "readingMinutes": 20,
  "operationMinutes": 45,
  "blocks": [
    {
      "type": "h2",
      "text": [
        "目標與準備",
        "Goal and preparation",
        "目標と準備",
        "목표와 준비"
      ]
    },
    {
      "type": "note",
      "text": [
        "做出一個保留原始資料的 CSV 整理工具：修剪空白、統一 email 大小寫、保留第一筆有效資料、统计重複與無效列，拒絕覆寫既有檔案。閱讀約 20 分鐘、實作約 45 分鐘。準備 Python 3、Codex 與自己的空白 csv-lab；只使用下方虛構姓名及 example.test 地址。這是格式整理，不會寄信，也不能證明 email 真正存在。",
        "Build a CSV cleaner that preserves input, trims whitespace, normalizes email case, keeps the first valid record, counts duplicate/invalid rows and refuses existing output files. Allow 20 minutes to read and 45 to practice. Prepare Python 3, Codex and your own empty csv-lab. Use only the fictional names and example.test addresses below. This cleans formatting; it neither sends mail nor proves an address exists.",
        "入力を保存したまま空白除去、email の小文字化、最初の有効行の保持、重複・無効行の集計、既存出力の上書き拒否を行う CSV 整理器を作ります。読む時間20分、実習45分です。Python 3、Codex、自分の空 csv-lab と下記架空名・example.test だけを使います。形式整理であり、送信や住所の実在確認ではありません。",
        "입력을 보존하면서 공백 제거·email 소문자화·첫 유효 행 유지·중복/무효 집계·기존 출력 덮어쓰기 거부를 하는 CSV 정리기를 만듭니다. 읽기 20분, 실습 45분입니다. Python 3·Codex·자신의 빈 csv-lab과 아래 가상 이름·example.test만 사용하세요. 형식 정리이며 메일을 보내거나 주소 실재를 증명하지 않습니다."
      ]
    },
    {
      "type": "h2",
      "text": [
        "步驟 1：建立可以人工驗算的資料",
        "Step 1: Create manually checkable data",
        "手順1：手で検算できるデータを作る",
        "1단계: 손으로 검산할 데이터 만들기"
      ]
    },
    {
      "type": "p",
      "text": [
        "在編輯器開啟 csv-lab，以 UTF-8 建立 contacts.csv，完整內容如下。第一列欄名必須正好是 name,email，順序也相同。保留 Alice 前後與第一筆 email 的空白，它們是測試條件。使用純文字編輯器檢查原文，避免試算表另存時換編碼或改值。Windows 執行 py -3 --version，macOS／Linux 執行 python3 --version，確認 Python 3 可用；本例不需要 pip、pandas 或資料庫。",
        "Open csv-lab in an editor and create UTF-8 contacts.csv exactly below. The header must be name,email in that order. Preserve the spaces around Alice and the first email as test conditions. Inspect with a text editor so spreadsheet re-saving does not change encoding or values. Check py -3 --version on Windows or python3 --version on macOS/Linux. This example needs no pip packages, pandas or database.",
        "csv-lab を開き、下記を UTF-8 contacts.csv に保存します。表頭は順序も含め name,email と完全一致させ、Alice と最初の email の前後空白を試験条件として残します。試算表の再保存でエンコードや値が変わらないよう純文字で確認します。Windows は py -3 --version、macOS／Linux は python3 --version で Python 3 を確認し、pip・pandas・資料庫は不要です。",
        "csv-lab을 열어 아래를 UTF-8 contacts.csv로 저장합니다. 헤더는 순서까지 name,email과 같아야 하며 Alice와 첫 email 앞뒤 공백을 시험 조건으로 유지하세요. 스프레드시트 재저장으로 인코딩·값이 바뀌지 않게 텍스트 편집기로 확인합니다. Windows는 py -3 --version, macOS/Linux는 python3 --version으로 Python 3를 확인하며 pip·pandas·데이터베이스는 필요 없습니다."
      ]
    },
    {
      "type": "code",
      "language": "csv",
      "label": [
        "contacts.csv（保留測試空白）",
        "contacts.csv (preserve test spaces)",
        "contacts.csv（試験用空白を保持）",
        "contacts.csv (시험 공백 유지)"
      ],
      "code": "name,email\n Alice , ALICE@EXAMPLE.TEST \nAlice duplicate,alice@example.test\nBob,bob@example.test\nMissing,\n"
    },
    {
      "type": "p",
      "text": [
        "| 輸入列 | 判定 | 輸出 |\n| --- | --- | --- |\n| Alice／ALICE@EXAMPLE.TEST | 有效，清除空白並轉小寫 | Alice／alice@example.test |\n| Alice duplicate／alice@example.test | 與有效 email 重複 | 不保留 |\n| Bob／bob@example.test | 有效 | Bob／bob@example.test |\n| Missing／空 email | 無效 | 不保留 |\n\n因此預期 kept 2、duplicate 1、invalid 1。去重依標準化後 email，不依姓名；若第一筆同地址資料因姓名空白而無效，後來第一筆有效資料仍可保留。這些規則應在生成程式前說清楚。",
        "| Input row | Decision | Output |\n| --- | --- | --- |\n| Alice / ALICE@EXAMPLE.TEST | Valid; trim and lowercase | Alice / alice@example.test |\n| Alice duplicate / alice@example.test | Duplicate valid email | Omit |\n| Bob / bob@example.test | Valid | Bob / bob@example.test |\n| Missing / blank email | Invalid | Omit |\n\nExpect kept 2, duplicate 1, invalid 1. Deduplicate normalized email, not names. If an earlier row for that address is invalid because its name is blank, the first later valid row can still be kept. Specify these rules before generating code.",
        "| 入力行 | 判定 | 出力 |\n| --- | --- | --- |\n| Alice／ALICE@EXAMPLE.TEST | 有効・空白除去・小文字化 | Alice／alice@example.test |\n| Alice duplicate／alice@example.test | 有効 email の重複 | 省略 |\n| Bob／bob@example.test | 有効 | Bob／bob@example.test |\n| Missing／空 email | 無効 | 省略 |\n\nkept 2・duplicate 1・invalid 1が期待値です。姓名ではなく正規化 email で重複を決めます。同住所の先行行が空の姓名で無効なら後の最初の有効行を残せます。生成前にこの規則を明確にします。",
        "| 입력 행 | 판정 | 출력 |\n| --- | --- | --- |\n| Alice / ALICE@EXAMPLE.TEST | 유효·공백 제거·소문자화 | Alice / alice@example.test |\n| Alice duplicate / alice@example.test | 유효 email 중복 | 제외 |\n| Bob / bob@example.test | 유효 | Bob / bob@example.test |\n| Missing / 빈 email | 무효 | 제외 |\n\nkept 2·duplicate 1·invalid 1을 기대합니다. 이름이 아니라 정규화한 email로 중복을 판정합니다. 같은 주소의 앞 행이 빈 이름으로 무효라면 뒤의 첫 유효 행을 유지할 수 있습니다. 생성 전에 이 규칙을 명확히 하세요."
      ]
    },
    {
      "type": "h2",
      "text": [
        "步驟 2：請 Codex 先規劃整理契約",
        "Step 2: Ask Codex to plan the cleaning contract",
        "手順2：整理契約を先に計画する",
        "2단계: 정리 계약 먼저 계획"
      ]
    },
    {
      "type": "p",
      "text": [
        "讓 Codex 開啟 csv-lab，先用 [Plan 模式](article:codex-plan-mode)確認輸入、輸出、例外及驗收，再交付實作。每次只輸出新檔案；結構壞掉要整次失敗，不留下看似成功的半份資料。一般無效 email 是可計數的資料問題，但缺欄、多欄或錯誤表頭是契約錯誤。這個區分會直接影響下游是否敢使用結果，不能只寫一句幫我清理 CSV。",
        "Open csv-lab in Codex and use [Plan mode](article:codex-plan-mode) to agree on input, output, exceptions and acceptance before implementing. Write only a new output file. Structural corruption must fail the run without a misleading partial result. An invalid email is countable bad data, while missing/extra columns or a wrong header violate the contract. Downstream use depends on that distinction, so ask for more than simply cleaning a CSV.",
        "Codex で csv-lab を開き、[Plan モード](article:codex-plan-mode)で入出力・例外・検証を確認してから実装します。出力は毎回新規で、構造損壊は成功に見える部分結果を残さず全体を失敗させます。無効 email は集計可能な資料問題ですが、欄不足・余分・表頭誤りは契約違反です。下流で使えるかに関わるため、単に CSV 整理と頼むだけにしません。",
        "Codex에서 csv-lab을 열고 [Plan 모드](article:codex-plan-mode)로 입출력·예외·검증을 확인한 뒤 구현합니다. 출력은 매번 새 파일이며 구조 손상은 성공처럼 보이는 일부 결과를 남기지 않고 전체 실패해야 합니다. 무효 email은 집계 가능한 자료 문제지만 필드 부족·추가·헤더 오류는 계약 위반입니다. 후속 사용 가능성에 영향을 주므로 단순히 CSV 정리라고만 요청하지 마세요."
      ]
    },
    {
      "type": "code",
      "language": "text",
      "label": [
        "實作需求提示詞",
        "Implementation request",
        "実装依頼",
        "구현 요청"
      ],
      "code": "Create clean_contacts.py using Python's standard library only.\nExpose clean(source: Path, target: Path) returning kept/duplicate/invalid counts.\nRead UTF-8 with optional BOM using csv.DictReader, not string splitting.\nRequire exactly name,email. Trim both fields and lowercase email.\nKeep the first valid row per email; reject empty names and invalid email shapes.\nTreat missing/extra columns or malformed CSV as whole-run failures.\nRead and validate before creating output. Never change input or overwrite output.\nWrite UTF-8 with name,email and a newline after each row.\nCLI takes source and target paths, reports counts on stderr and exits 0 on success,\n1 on data/file errors. Add tests for preservation, duplicates, Unicode, quoted\ncommas, malformed rows and overwrite refusal. Do not use real contact data.\n"
    },
    {
      "type": "h2",
      "text": [
        "步驟 3：檢查參考程式的保護位置",
        "Step 3: Inspect the reference safeguards",
        "手順3：参考実装の保護箇所を見る",
        "3단계: 참고 구현의 보호 지점 확인"
      ]
    },
    {
      "type": "p",
      "text": [
        "以下是完整參考程式，存為 clean_contacts.py 可獨立執行；若你已讓 Codex 生成版本，先另存備份，再比較差異。csv 模組負責引號、逗號與換行，不能用 split(',') 替代。程式在全部輸入讀完後才用 x 模式建立輸出；已有目標檔就失敗。這能避免輸入格式錯誤時留下輸出，但磁碟在寫到一半失敗仍可能留下不完整檔案，該次必須標記失敗並檢查產物，不能聲稱這是完整交易系統。",
        "The complete reference below can run independently as clean_contacts.py. Back up your generated version before comparing. The csv module handles quotes, commas and newlines; split(',') cannot replace it. After reading all input, the program creates output with x mode, refusing existing targets. This avoids output on malformed input, but a disk failure during writing can still leave a partial file. Mark that run failed and inspect its artifacts; this is not a transactional system.",
        "下記は clean_contacts.py として単独実行できる全文です。生成版があれば先に別保存して比較します。csv 模組は引用符・カンマ・改行を扱い、split(',') では代替できません。全入力を読んだ後 x で新規出力を作り、既存目標を拒否します。入力誤りでは出力を残しませんが、書込途中のディスク障害なら部分ファイルが残り得るため失敗と記録して調査します。完全なトランザクションシステムとは称しません。",
        "아래는 clean_contacts.py로 독립 실행하는 전체 참고 코드입니다. 생성본은 별도 보관한 뒤 비교하세요. csv 모듈이 따옴표·쉼표·줄바꿈을 처리하며 split(',')로 대체할 수 없습니다. 전체 입력을 읽은 뒤 x로 새 출력을 만들고 기존 대상은 거부합니다. 입력 오류에는 출력이 없지만 쓰는 도중 디스크 오류는 일부 파일을 남길 수 있어 실패로 기록하고 조사해야 합니다. 완전한 트랜잭션 시스템이라고 부르지 않습니다."
      ]
    },
    {
      "type": "code",
      "language": "python",
      "label": [
        "clean_contacts.py（完整參考）",
        "clean_contacts.py (complete reference)",
        "clean_contacts.py（全参考実装）",
        "clean_contacts.py (전체 참고 코드)"
      ],
      "code": "\"\"\"Clean a practice CSV without overwriting either the input or an existing output.\"\"\"\nimport argparse\nimport csv\nimport json\nimport re\nimport sys\nfrom pathlib import Path\n\n\ndef clean(source: Path, target: Path) -> dict[str, int]:\n    if source.resolve() == target.resolve():\n        raise ValueError(\"Input and output must differ\")\n    rows = []\n    seen = set()\n    counts = {\"kept\": 0, \"duplicate\": 0, \"invalid\": 0}\n    with source.open(encoding=\"utf-8-sig\", newline=\"\") as stream:\n        reader = csv.DictReader(stream, strict=True)\n        if reader.fieldnames != [\"name\", \"email\"]:\n            raise ValueError(\"Expected exactly: name,email\")\n        for row in reader:\n            if None in row or any(value is None for value in row.values()):\n                raise ValueError(\"Malformed CSV row\")\n            name = row[\"name\"].strip()\n            email = row[\"email\"].strip().lower()\n            # An exercise-level shape check, not proof an address can receive mail.\n            if not name or not re.fullmatch(r\"[^\\s@]+@[^\\s@]+\\.[^\\s@]+\", email):\n                counts[\"invalid\"] += 1\n            elif email in seen:\n                counts[\"duplicate\"] += 1\n            else:\n                seen.add(email)\n                rows.append({\"name\": name, \"email\": email})\n                counts[\"kept\"] += 1\n    # Exclusive creation: a rerun never silently replaces an existing result.\n    with target.open(\"x\", encoding=\"utf-8\", newline=\"\") as stream:\n        writer = csv.DictWriter(stream, fieldnames=[\"name\", \"email\"], lineterminator=\"\\n\")\n        writer.writeheader()\n        writer.writerows(rows)\n    return counts\n\n\ndef main() -> int:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"source\", type=Path)\n    parser.add_argument(\"target\", type=Path)\n    args = parser.parse_args()\n    try:\n        counts = clean(args.source, args.target)\n    except (OSError, ValueError, csv.Error) as error:\n        print(str(error), file=sys.stderr)\n        return 1\n    print(json.dumps(counts), file=sys.stderr)\n    return 0\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(main())\n"
    },
    {
      "type": "h2",
      "text": [
        "步驟 4：執行、保存狀態與核對輸出",
        "Step 4: Run, capture status and verify output",
        "手順4：実行・状態保存・出力確認",
        "4단계: 실행·상태 저장·출력 확인"
      ]
    },
    {
      "type": "p",
      "text": [
        "執行前確認 cleaned-01.csv 不存在。在 Windows PowerShell 記下輸入 SHA256；macOS／Linux 複製一份 contacts-before.csv 供 cmp 比較。這些備份都是自己剛建立的虛構檔案。清理程式的計數寫在 stderr，所以成功時終端機也會顯示一行 JSON；這不是失敗訊息。退出狀態要立即保存，再開啟輸出與輸入核對。",
        "Confirm cleaned-01.csv does not exist. Record the input SHA256 in Windows PowerShell, or copy contacts-before.csv for cmp on macOS/Linux. These are your own new fictional files. Counts go to stderr, so success also displays one JSON line; that is not an error. Capture exit status immediately, then inspect output and compare input.",
        "cleaned-01.csv が未作成か確認し、Windows は入力 SHA256、macOS／Linux は contacts-before.csv のコピーを cmp 用に用意します。全て自分の架空ファイルです。集計は stderr に出るため成功でも JSON が1行表示され、エラーではありません。直後に終了状態を保存し出力と入力を確認します。",
        "cleaned-01.csv가 없는지 확인하고 Windows는 입력 SHA256, macOS/Linux는 cmp용 contacts-before.csv 복사본을 준비합니다. 모두 자신이 만든 가상 파일입니다. 집계는 stderr에 출력되어 성공해도 JSON 한 줄이 보이며 오류가 아닙니다. 종료 상태를 즉시 저장한 뒤 출력·입력을 확인하세요."
      ]
    },
    {
      "type": "code",
      "language": "powershell",
      "label": [
        "Windows PowerShell",
        "Windows PowerShell",
        "Windows PowerShell",
        "Windows PowerShell"
      ],
      "code": "$csvBefore = (Get-FileHash -LiteralPath contacts.csv -Algorithm SHA256).Hash\npy -3 clean_contacts.py contacts.csv cleaned-01.csv\n$csvExit = $LASTEXITCODE\n$csvExit\nGet-Content -LiteralPath cleaned-01.csv\n$csvBefore -eq (Get-FileHash -LiteralPath contacts.csv -Algorithm SHA256).Hash\n"
    },
    {
      "type": "code",
      "language": "sh",
      "label": [
        "macOS／Linux",
        "macOS / Linux",
        "macOS／Linux",
        "macOS / Linux"
      ],
      "code": "cp contacts.csv contacts-before.csv\npython3 clean_contacts.py contacts.csv cleaned-01.csv\ncsv_exit=$?\necho \"$csv_exit\"\ncat cleaned-01.csv\ncmp contacts-before.csv contacts.csv\n"
    },
    {
      "type": "code",
      "language": "csv",
      "label": [
        "cleaned-01.csv 的預期內容",
        "Expected cleaned-01.csv",
        "cleaned-01.csv の期待内容",
        "cleaned-01.csv 예상 내용"
      ],
      "code": "name,email\nAlice,alice@example.test\nBob,bob@example.test\n"
    },
    {
      "type": "p",
      "text": [
        "預期退出 0，計數為 kept 2、duplicate 1、invalid 1，輸出兩筆且順序保留，PowerShell 比對 True 或 cmp 無差異。接著原指令再執行一次，因 cleaned-01.csv 已存在應退出 1，既有輸出內容不變；不要先刪掉它來假装通過覆寫防護。再把輸入與輸出都指定 contacts.csv，應拒絕且原件不變。這些失敗是驗收成功的一部分。",
        "Expect exit 0, counts kept 2, duplicate 1, invalid 1, two output records in original order, and True or no cmp difference. Repeat the same command: existing cleaned-01.csv must cause exit 1 without changing it. Do not delete it first and claim overwrite protection passed. Then use contacts.csv as both input and output; it must refuse without changing the original. These expected failures are part of successful acceptance.",
        "0終了、kept 2・duplicate 1・invalid 1、元順序の2件、True または cmp 差異なしを期待します。同じ命令を再実行すると既存 cleaned-01.csv により1終了し、内容不変であるべきです。先に削除して上書き保護合格としません。入出力を両方 contacts.csv にした場合も原本不変で拒否します。この期待された失敗も検証合格の一部です。",
        "종료 0, kept 2·duplicate 1·invalid 1, 원래 순서의 두 행, True 또는 cmp 차이 없음을 기대합니다. 같은 명령을 반복하면 기존 cleaned-01.csv 때문에 종료 1이며 내용은 그대로여야 합니다. 먼저 삭제하고 덮어쓰기 보호를 통과했다고 하지 마세요. 입출력을 모두 contacts.csv로 지정해도 원본 불변으로 거부해야 합니다. 예상 실패도 검증 통과의 일부입니다."
      ]
    },
    {
      "type": "h2",
      "text": [
        "步驟 5：Unicode、引號與壞列",
        "Step 5: Unicode, quotes and malformed rows",
        "手順5：Unicode・引用符・壊れた行",
        "5단계: Unicode·따옴표·잘못된 행"
      ]
    },
    {
      "type": "p",
      "text": [
        "另存下方 contacts-unicode.csv，輸出用 cleaned-unicode.csv。預期 kept 1、duplicate 0、invalid 0，姓名中的逗號仍屬同一欄，Unicode 不變。若輸出重新打開變成亂碼，先確認編輯器以 UTF-8 解碼，不立刻改寫原始輸入。再建立 bad.csv，內容為 name,email 換行 Alice，也就是缺少 email 欄；指定全新的 cleaned-bad.csv 執行，應退出 1 且不建立輸出。把表頭改成 email,name 或多加第三欄也應拒絕。",
        "Create contacts-unicode.csv below and output cleaned-unicode.csv. Expect kept 1, duplicate 0, invalid 0, with the comma inside one name field and Unicode preserved. If re-opening looks garbled, check UTF-8 decoding before rewriting input. Create bad.csv containing name,email followed by a newline and Alice, a missing email column. A new cleaned-bad.csv target must not be created and exit must be 1. Reversed headers or an extra third column must also fail.",
        "下記 contacts-unicode.csv を作り、cleaned-unicode.csv に出します。kept 1・duplicate 0・invalid 0、姓名のカンマは同じ欄、Unicode 不変を期待します。再表示が文字化けなら入力を書き換える前に UTF-8 デコードを確認します。bad.csv は name,email の次行を Alice のみにして欄不足を作り、新規 cleaned-bad.csv は作らず1終了することを確認します。逆順表頭や第三欄も拒否すべきです。",
        "아래 contacts-unicode.csv를 만들어 cleaned-unicode.csv로 출력합니다. kept 1·duplicate 0·invalid 0이며 이름의 쉼표는 한 필드 안에 있고 Unicode가 유지되어야 합니다. 다시 열 때 깨지면 입력을 고치기 전에 UTF-8 해석을 확인하세요. bad.csv는 name,email 다음 줄에 Alice만 써 email 필드를 누락시킵니다. 새 cleaned-bad.csv가 생성되지 않고 종료 1이어야 합니다. 뒤집은 헤더·세 번째 필드 추가도 거부해야 합니다."
      ]
    },
    {
      "type": "code",
      "language": "csv",
      "label": [
        "contacts-unicode.csv",
        "contacts-unicode.csv",
        "contacts-unicode.csv",
        "contacts-unicode.csv"
      ],
      "code": "name,email\n\"林, Alex\",alex@example.test\n"
    },
    {
      "type": "h2",
      "text": [
        "步驟 6：把驗收留成可重跑測試",
        "Step 6: Retain repeatable acceptance tests",
        "手順6：繰返せる検証を保存する",
        "6단계: 재실행 가능한 검증 보존"
      ]
    },
    {
      "type": "p",
      "text": [
        "把下方完整測試存為 test_clean_contacts.py，放在程式與 contacts.csv 旁邊。它使用暫存資料夾，會核對輸入位元組未變、既有輸出拒絕覆寫、Unicode、引號與結構錯誤；不在你的練習目錄累積測試產物。Windows 執行 py -3 -m unittest -v test_clean_contacts.py；macOS／Linux 執行 python3 -m unittest -v test_clean_contacts.py。參考版本應有三項測試全部通過；若生成版本的介面不同，先對齊 clean 函式契約，不隨意刪除失敗案例。",
        "Save the complete test as test_clean_contacts.py beside the program and contacts.csv. It uses temporary folders to check byte-preserved input, overwrite refusal, Unicode, quoting and structural errors without accumulating test outputs in your practice folder. Run py -3 -m unittest -v test_clean_contacts.py on Windows or python3 -m unittest -v test_clean_contacts.py on macOS/Linux. All three reference tests should pass. If your generated interface differs, align the clean function contract rather than deleting failing cases.",
        "下記を程式と contacts.csv の横に test_clean_contacts.py として保存します。一時場所で入力位元組不変、上書き拒否、Unicode、引用符、構造エラーを検証し、練習ディレクトリに産物を溜めません。Windows は py -3 -m unittest -v test_clean_contacts.py、macOS／Linux は python3 -m unittest -v test_clean_contacts.py で参考3試験全合格を確認します。生成版の介面が違えば clean 契約を揃え、失敗例を削除しません。",
        "아래를 프로그램·contacts.csv 옆의 test_clean_contacts.py로 저장합니다. 임시 폴더에서 입력 바이트 불변·덮어쓰기 거부·Unicode·따옴표·구조 오류를 검증해 연습 폴더에 결과를 쌓지 않습니다. Windows는 py -3 -m unittest -v test_clean_contacts.py, macOS/Linux는 python3 -m unittest -v test_clean_contacts.py로 참고 시험 세 개 통과를 확인하세요. 생성본 인터페이스가 다르면 clean 계약을 맞추고 실패 사례를 삭제하지 않습니다."
      ]
    },
    {
      "type": "code",
      "language": "python",
      "label": [
        "test_clean_contacts.py",
        "test_clean_contacts.py",
        "test_clean_contacts.py",
        "test_clean_contacts.py"
      ],
      "code": "import csv\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nfrom clean_contacts import clean\n\n\nclass CleanupTests(unittest.TestCase):\n    def test_contract_and_input_preservation(self):\n        original = (Path(__file__).parent / \"contacts.csv\").read_bytes()\n        with tempfile.TemporaryDirectory() as directory:\n            source = Path(directory) / \"input.csv\"\n            target = Path(directory) / \"output.csv\"\n            source.write_bytes(original)\n            self.assertEqual(clean(source, target), {\"kept\": 2, \"duplicate\": 1, \"invalid\": 1})\n            self.assertEqual(source.read_bytes(), original)\n            with target.open(encoding=\"utf-8\", newline=\"\") as stream:\n                self.assertEqual(list(csv.DictReader(stream)), [{\"name\": \"Alice\", \"email\": \"alice@example.test\"}, {\"name\": \"Bob\", \"email\": \"bob@example.test\"}])\n            with self.assertRaises(FileExistsError):\n                clean(source, target)\n            with self.assertRaises(ValueError):\n                clean(source, source)\n\n    def test_unicode_and_quoted_comma(self):\n        with tempfile.TemporaryDirectory() as directory:\n            source, target = Path(directory) / \"in.csv\", Path(directory) / \"out.csv\"\n            source.write_text('name,email\\n\"林, Alex\",alex@example.test\\n', encoding=\"utf-8\")\n            self.assertEqual(clean(source, target)[\"kept\"], 1)\n            self.assertIn('\"林, Alex\"', target.read_text(encoding=\"utf-8\"))\n\n    def test_bad_header_and_malformed_rows_create_no_output(self):\n        with tempfile.TemporaryDirectory() as directory:\n            source, target = Path(directory) / \"in.csv\", Path(directory) / \"out.csv\"\n            for value in [\"email,name\\na@b.test,A\\n\", \"name,email\\nAlice\\n\", \"name,email\\nA,a@b.test,extra\\n\"]:\n                source.write_text(value, encoding=\"utf-8\")\n                with self.assertRaises(ValueError):\n                    clean(source, target)\n                self.assertFalse(target.exists())\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n"
    },
    {
      "type": "h2",
      "text": [
        "交付、還原與限制",
        "Delivery, recovery and limits",
        "引渡し・復元・制限",
        "전달·복원·제한"
      ]
    },
    {
      "type": "p",
      "text": [
        "交付時提供原始虛構 CSV、程式、測試、執行指令、計數及未修改原件的證據。可以下載[完整 CSV 練習檔](https://mokaair.com/guides/codex-csv-workshop/contacts-practice.zip)比對；壓縮檔包含參考程式，不需要先完成其他專案。重新整理用新的輸出名稱，清理時只選自己已確認的產物。若要讀真正名單、處理超大檔案或匯入試算表，另設計權限、串流及公式文字的處理規則；本篇把資料當文字保存，不自動把外部內容當命令執行。下一篇練習[接手既有專案](article:codex-maintain-existing-project)，保留這裡建立的基準與驗證方法。",
        "Deliver the fictional source CSV, program, tests, command, counts and evidence of preserved input. Download the [complete CSV exercise](https://mokaair.com/guides/codex-csv-workshop/contacts-practice.zip) for comparison; it includes reference code and needs no earlier project. Use new output names for reruns and clean only identified artifacts. Real contacts, very large files or spreadsheet imports need separate access, streaming and formula-text rules. This lesson saves data as text and never executes external content as commands. Continue to [Maintaining an existing project](article:codex-maintain-existing-project) with the same baseline and verification discipline.",
        "架空入力、程式、試験、命令、計数、原本保持の証拠を引渡します。[CSV 教材一式](https://mokaair.com/guides/codex-csv-workshop/contacts-practice.zip)には参考コードがあり前のプロジェクトなしで比較できます。再整理は新しい出力名、整理は確認済み産物だけです。実名簿、大ファイル、試算表インポートは権限・ストリーミング・公式文字の規則を別設計します。本編は文字として保存し外部内容を命令実行しません。この基準と検証法で[既存プロジェクトの保守](article:codex-maintain-existing-project)へ進みます。",
        "가상 입력·프로그램·시험·명령·집계·원본 보존 증거를 전달합니다. [CSV 전체 자료](https://mokaair.com/guides/codex-csv-workshop/contacts-practice.zip)는 참고 코드가 있어 이전 프로젝트 없이 비교할 수 있습니다. 재정리는 새 출력 이름, 정리는 확인한 결과만 선택합니다. 실제 연락처·대용량·스프레드시트 가져오기는 권한·스트리밍·수식 문자열 규칙을 별도로 설계하세요. 이 편은 텍스트로 저장하며 외부 내용을 명령으로 실행하지 않습니다. 같은 기준·검증법으로 [기존 프로젝트 유지보수](article:codex-maintain-existing-project)로 진행합니다."
      ]
    }
  ]
}
