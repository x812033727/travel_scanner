{
  "id": 55,
  "readingMinutes": 20,
  "operationMinutes": 25,
  "blocks": [
    {
      "type": "h2",
      "text": [
        "目標與材料",
        "Goal and materials",
        "目標と教材",
        "목표와 자료"
      ]
    },
    {
      "type": "note",
      "text": [
        "把一次 Codex 回答變成能被程式檢查的資料，同時保留執行事件。閱讀約 20 分鐘、操作約 25 分鐘，模型等待另計。需要 [codex exec](article:codex-exec-scripting)、已登入的 CLI、Git 與 Python 3。這次模型執行會使用額度；故意破壞輸出再驗證的步驟只在本機讀檔，不會再次呼叫模型。",
        "Turn a Codex answer into programmatically checked data while retaining execution events. Allow 20 minutes to read and 25 to practice, plus model waiting. You need [codex exec](article:codex-exec-scripting), an authenticated CLI, Git and Python 3. The model run uses quota; deliberately damaging and revalidating output only reads local files and does not call the model again.",
        "Codex の回答をプログラムで検証できるデータにし、実行イベントも残します。読む時間20分、実習25分にモデル待機が加わります。[codex exec](article:codex-exec-scripting)、認証済み CLI、Git、Python 3 が必要です。モデル実行は利用枠を使いますが、出力を意図的に壊して再検証する手順はローカルの読取りのみで再要求しません。",
        "Codex 답변을 프로그램으로 검증하는 데이터로 만들고 실행 이벤트도 보존합니다. 읽기 20분, 실습 25분에 모델 대기가 추가됩니다. [codex exec](article:codex-exec-scripting), 인증된 CLI, Git, Python 3가 필요합니다. 모델 실행은 사용량을 쓰지만 출력을 일부러 손상해 재검증하는 단계는 로컬 파일만 읽으며 모델을 다시 호출하지 않습니다."
      ]
    },
    {
      "type": "h2",
      "text": [
        "步驟 1：分開事件與最後答案",
        "Step 1: Separate events from the final answer",
        "手順1：イベントと最終回答を分ける",
        "1단계: 이벤트와 최종 답변 분리"
      ]
    },
    {
      "type": "p",
      "text": [
        "--json 會把 stdout 改為 JSONL，也就是每一行各自是一個 JSON 物件。它不是單一 JSON 陣列，也不是只包含最後答案；不能直接對整份 events.jsonl 呼叫一次 json.loads。--output-schema 指定最後回答的結構，-o 把最後回答另存為 final.json。因此同一工作可以同時保留事件與結構化答案，stderr 則繼續保存診斷。副檔名只是名稱，真正格式由旗標與內容決定。",
        "--json changes stdout to JSONL: each line is a separate JSON object. It is neither a single JSON array nor just the final answer, so do not call json.loads once on the whole events.jsonl. --output-schema specifies the final response shape, and -o saves that response as final.json. One run can retain both events and a structured answer, while stderr retains diagnostics. Extensions are names; flags and content determine the actual format.",
        "--json は stdout を JSONL、つまり1行ごとに独立した JSON オブジェクトにします。単一の配列でも最終回答だけでもないので、events.jsonl 全体を1回の json.loads に渡しません。--output-schema は最終回答の形を指定し、-o はその回答を final.json に保存します。1回でイベントと構造化回答を残し、stderr に診断を保存できます。拡張子は名前であり、形式はフラグと内容で決まります。",
        "--json은 stdout을 한 줄마다 독립된 JSON 객체인 JSONL로 바꿉니다. 하나의 배열이나 최종 답변만이 아니므로 events.jsonl 전체에 json.loads를 한 번 호출하면 안 됩니다. --output-schema는 최종 답변 구조를 지정하고 -o는 이를 final.json에 저장합니다. 한 번 실행으로 이벤트·구조화 답변을 모두 보관하며 stderr는 진단을 저장합니다. 확장자는 이름이며 실제 형식은 플래그와 내용이 결정합니다."
      ]
    },
    {
      "type": "p",
      "text": [
        "| 檔案 | 內容 | 接受條件 |\n| --- | --- | --- |\n| status.json | 包裝程式記錄的退出狀態 | exited 且 exit_code 為 0 |\n| events.jsonl | CLI 的逐行事件 | 可逐行解析，完成事件存在且無失敗事件 |\n| final.json | 最後的結構化回答 | 欄位、型別、版本與數值可驗證 |\n| stderr.log | 過程與錯誤說明 | 供診斷，不當成答案 |\n| schema.json | 本輪要求的格式 | 與預期契約一起保存 |\n\n表中的檔名是本教材約定，並非 Codex 固定自動產生的全部檔案。",
        "| File | Content | Acceptance condition |\n| --- | --- | --- |\n| status.json | Wrapper's exit record | exited with exit_code 0 |\n| events.jsonl | Line-oriented CLI events | Parseable lines, a completion and no failure event |\n| final.json | Final structured response | Check fields, types, revision and values |\n| stderr.log | Progress and diagnostics | Investigate it; do not treat it as the answer |\n| schema.json | Format requested for this run | Keep it with the expected contract |\n\nThese filenames are exercise conventions, not a list of files Codex always creates automatically.",
        "| ファイル | 内容 | 合格条件 |\n| --- | --- | --- |\n| status.json | ラッパーが記録した終了状態 | exited かつ exit_code 0 |\n| events.jsonl | CLI の行単位イベント | 各行を解析でき、完了があり失敗がない |\n| final.json | 最終構造化回答 | 欄・型・版・値を検証 |\n| stderr.log | 進行と診断 | 調査用で回答ではない |\n| schema.json | 今回要求した形式 | 期待する契約と保存 |\n\nこれらの名前は教材の約束で、Codex が常に自動作成するファイル一覧ではありません。",
        "| 파일 | 내용 | 승인 조건 |\n| --- | --- | --- |\n| status.json | 래퍼의 종료 기록 | exited이며 exit_code 0 |\n| events.jsonl | 줄 단위 CLI 이벤트 | 각 줄 해석 가능, 완료 존재, 실패 없음 |\n| final.json | 최종 구조화 답변 | 필드·타입·버전·값 검증 |\n| stderr.log | 진행·진단 | 조사 자료이며 답변 아님 |\n| schema.json | 이번에 요구한 형식 | 예상 계약과 함께 보존 |\n\n이 파일명은 실습 규칙이며 Codex가 항상 자동 생성하는 파일 목록이 아닙니다."
      ]
    },
    {
      "type": "h2",
      "text": [
        "步驟 2：準備固定的輸入與契約",
        "Step 2: Prepare a fixed input and contract",
        "手順2：固定の入力と契約を用意する",
        "2단계: 고정 입력과 계약 준비"
      ]
    },
    {
      "type": "p",
      "text": [
        "在自己的空白 exec-json-lab 資料夾執行 git init，以 UTF-8 建立下方 tasks.md。若你沿用前篇資料，先確認內容完全一致；本篇驗證器刻意只接受 exec-practice-1，避免拿其他版本的回答混用。先人工計算總數 3、完成 1、未完成 2。需要改用真實資料時，應另訂版本與驗收契約，不是刪掉驗證直到任何輸出都能通過。",
        "Run git init in your own empty exec-json-lab folder and create UTF-8 tasks.md below. If reusing the previous lesson's fixture, verify it is identical. This validator deliberately accepts only exec-practice-1 to prevent mixing revisions. Count total 3, completed 1 and pending 2 by hand. For real data, define a new revision and acceptance contract instead of removing checks until any output passes.",
        "自分の空の exec-json-lab で git init を実行し、下記 tasks.md を UTF-8 で作成します。前編の教材を使うなら完全一致を確認します。異なる版の混用を防ぐため検証器は exec-practice-1 だけを受け付けます。手で総数3、完了1、未完了2を確認します。実データに変える場合は新しい版と検証契約を定義し、何でも通るまで検証を削除しません。",
        "자신의 빈 exec-json-lab에서 git init을 실행하고 아래 UTF-8 tasks.md를 만드세요. 이전 자료를 재사용하면 완전히 같은지 확인합니다. 버전 혼동을 막기 위해 검증기는 exec-practice-1만 허용합니다. 전체 3, 완료 1, 미완료 2를 손으로 확인하세요. 실제 자료에는 새 버전·승인 계약을 정의하며 모든 출력이 통과하도록 검사를 지우지 않습니다."
      ]
    },
    {
      "type": "code",
      "language": "markdown",
      "label": [
        "tasks.md",
        "tasks.md",
        "tasks.md",
        "tasks.md"
      ],
      "code": "# Practice tasks\nRevision: exec-practice-1\n\n- [x] Read the guide\n- [ ] Create a practice file\n- [ ] Verify the result\n"
    },
    {
      "type": "h2",
      "text": [
        "步驟 3：建立一次執行、逐層驗證的程式",
        "Step 3: Create a single-run, layered validator",
        "手順3：1回実行して段階的に検証するプログラム",
        "3단계: 한 번 실행하고 단계별 검증하는 프로그램"
      ]
    },
    {
      "type": "p",
      "text": [
        "將下方完整程式存為同一資料夾的 run_summary.py。它只用 Python 標準函式庫，不需 pip install。程式先建立全新的輸出資料夾，保存 schema 與 running 狀態，再用參數陣列啟動 Codex；不把模型文字拼成 shell 指令。120 秒是教材自行設定的等待上限，不是 Codex 的固定限制。遇到逾時、啟動失敗或非零退出，保留產物並拒絕接受答案，不自動重試。",
        "Save the complete program below as run_summary.py in that folder. It uses only Python's standard library, with no pip install. It creates a new output directory, records the schema and running state, then starts Codex with an argument array, never a shell command assembled from model text. The 120-second timeout is an exercise choice, not a fixed Codex limit. Timeout, launch failure or nonzero exit retains evidence and rejects the answer without automatic retry.",
        "下記全体を同じフォルダーの run_summary.py に保存します。標準ライブラリだけなので pip install は不要です。新しい出力フォルダー、schema、running 状態を保存してから引数配列で Codex を起動し、モデル文字列から shell 命令を組み立てません。120秒は教材の設定で Codex 固定制限ではありません。タイムアウト・起動失敗・非0終了は証拠を残して回答を不採用とし、自動再試行しません。",
        "아래 전체 프로그램을 같은 폴더의 run_summary.py로 저장하세요. Python 표준 라이브러리만 쓰므로 pip install이 필요 없습니다. 새 출력 폴더·schema·running 상태를 저장한 뒤 인수 배열로 Codex를 시작하며 모델 텍스트로 shell 명령을 조립하지 않습니다. 120초는 교육용 제한이며 Codex 고정 한도가 아닙니다. 시간 초과·시작 실패·0이 아닌 종료는 증거를 남기고 답변을 거부하며 자동 재시도하지 않습니다."
      ]
    },
    {
      "type": "p",
      "text": [
        "Windows 本篇使用獨立安裝版 codex.exe。在 PowerShell 先執行 Get-Command codex -All，確認來源是已安裝的 .exe；若只有 npm 的 .cmd／.ps1 包裝檔或別名，Python 不會套用 PowerShell 的命令解析。請依 [Windows 安裝](article:codex-cli-windows) 使用官方獨立安裝管道，開新終端機後重查；不要為此改成 shell=True 或拼接 shell 指令。macOS／Linux 則確認終端機和 Python 程序使用相同 PATH。",
        "On Windows this lesson uses the standalone codex.exe. Run Get-Command codex -All in PowerShell and confirm an installed .exe. If it lists only npm .cmd/.ps1 wrappers or an alias, Python does not use PowerShell command resolution. Follow the official standalone option in [Windows installation](article:codex-cli-windows), reopen the terminal and check again. Do not switch to shell=True or concatenate shell commands to fix this. On macOS/Linux ensure the terminal and Python process use the same PATH.",
        "Windows では単体版の codex.exe を使います。PowerShell で Get-Command codex -All を実行し、インストール済み .exe を確認します。npm の .cmd／.ps1 ラッパーや別名しかない場合、Python は PowerShell と同じ方法でコマンドを探しません。[Windows のインストール](article:codex-cli-windows)から公式の単体版を導入し、新しいターミナルで再確認します。対処として shell=True やシェル文字列の連結に変更しません。macOS／Linux はターミナルと Python の PATH が同じことを確認します。",
        "Windows에서는 독립 설치판 codex.exe를 사용합니다. PowerShell에서 Get-Command codex -All을 실행해 설치된 .exe인지 확인합니다. npm .cmd/.ps1 래퍼나 별칭만 있으면 Python은 PowerShell의 명령 검색 방식을 적용하지 않습니다. [Windows 설치](article:codex-cli-windows)의 공식 독립 설치 방법을 따르고 새 터미널에서 다시 확인하세요. 이를 해결하려고 shell=True나 셸 문자열 연결로 바꾸지 마세요. macOS/Linux에서는 터미널과 Python 프로세스의 PATH가 같은지 확인합니다."
      ]
    },
    {
      "type": "code",
      "language": "python",
      "label": [
        "run_summary.py（完整程式）",
        "run_summary.py (complete program)",
        "run_summary.py（全体）",
        "run_summary.py (전체 코드)"
      ],
      "code": "\"\"\"Run once, retain evidence, and accept only a validated practice summary.\"\"\"\nimport argparse\nimport json\nfrom pathlib import Path\nimport subprocess\nimport sys\n\nSCHEMA = {\n    \"type\": \"object\",\n    \"properties\": {\n        \"revision\": {\"type\": \"string\"},\n        \"total\": {\"type\": \"integer\", \"minimum\": 0},\n        \"completed\": {\"type\": \"integer\", \"minimum\": 0},\n        \"pending\": {\"type\": \"integer\", \"minimum\": 0},\n    },\n    \"required\": [\"revision\", \"total\", \"completed\", \"pending\"],\n    \"additionalProperties\": False,\n}\nPROMPT = (\n    \"Read only tasks.md. Return its Revision marker and checkbox counts \"\n    \"as revision, total, completed and pending. Do not edit input files \"\n    \"or use external services.\"\n)\n\n\ndef verify(run):\n    status = json.loads((run / \"status.json\").read_text(encoding=\"utf-8\"))\n    if (not isinstance(status, dict)\n            or status != {\"state\": \"exited\", \"exit_code\": 0}\n            or type(status.get(\"exit_code\")) is not int):\n        raise ValueError(\"Process did not exit successfully\")\n    events = []\n    for number, line in enumerate((run / \"events.jsonl\").read_text(encoding=\"utf-8-sig\").splitlines(), 1):\n        if not line.strip():\n            continue\n        event = json.loads(line)\n        if not isinstance(event, dict) or not isinstance(event.get(\"type\"), str):\n            raise ValueError(f\"Invalid event on line {number}\")\n        events.append(event[\"type\"])\n    if (events.count(\"turn.completed\") != 1 or events.count(\"turn.started\") != 1\n            or events.index(\"turn.started\") > events.index(\"turn.completed\")):\n        raise ValueError(\"Missing or ambiguous completed turn\")\n    if \"turn.failed\" in events or \"error\" in events:\n        raise ValueError(\"Failure event requires investigation\")\n    result = json.loads((run / \"final.json\").read_text(encoding=\"utf-8-sig\"))\n    if not isinstance(result, dict) or set(result) != set(SCHEMA[\"required\"]):\n        raise ValueError(\"Unexpected or missing final fields\")\n    if result[\"revision\"] != \"exec-practice-1\":\n        raise ValueError(\"Unexpected input revision\")\n    if any(type(result[key]) is not int or result[key] < 0 for key in (\"total\", \"completed\", \"pending\")):\n        raise ValueError(\"Counts must be nonnegative integers, not booleans\")\n    if result[\"total\"] != result[\"completed\"] + result[\"pending\"]:\n        raise ValueError(\"Inconsistent count arithmetic\")\n    return result\n\n\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument(\"run_name\")\n    parser.add_argument(\"--verify-only\", action=\"store_true\")\n    args = parser.parse_args()\n    root = Path.cwd().resolve()\n    if Path(args.run_name).name != args.run_name or args.run_name in {\".\", \"..\"}:\n        parser.error(\"Use a folder name, not a path\")\n    run = root / args.run_name\n    if not args.verify_only:\n        if not (root / \"tasks.md\").is_file():\n            parser.error(\"Missing tasks.md in the current folder\")\n        run.mkdir()  # Refuse to overwrite an earlier run.\n        schema_path = run / \"schema.json\"\n        schema_path.write_text(json.dumps(SCHEMA, indent=2) + \"\\n\", encoding=\"utf-8\")\n        status_path = run / \"status.json\"\n        status_path.write_text(json.dumps({\"state\": \"running\"}) + \"\\n\", encoding=\"utf-8\")\n        command = [\"codex\", \"exec\", \"--sandbox\", \"read-only\", \"--ephemeral\", \"--json\",\n                   \"--output-schema\", str(schema_path), \"-o\", str(run / \"final.json\"), PROMPT]\n        try:\n            with (run / \"events.jsonl\").open(\"xb\") as stdout, (run / \"stderr.log\").open(\"xb\") as stderr:\n                process = subprocess.run(command, cwd=root, stdout=stdout, stderr=stderr, timeout=120, check=False)\n            status = {\"state\": \"exited\", \"exit_code\": process.returncode}\n        except subprocess.TimeoutExpired:\n            status = {\"state\": \"timeout\"}\n        except OSError:\n            status = {\"state\": \"launch_failed\"}\n        status_path.write_text(json.dumps(status) + \"\\n\", encoding=\"utf-8\")\n    result = verify(run)\n    # Output data only: never execute text returned by the model.\n    print(json.dumps(result, ensure_ascii=False))\n\n\nif __name__ == \"__main__\":\n    try:\n        main()\n    except (OSError, ValueError) as error:\n        print(f\"Not accepted: {error}\", file=sys.stderr)\n        sys.exit(1)\n"
    },
    {
      "type": "h2",
      "text": [
        "步驟 4：執行並核對內容",
        "Step 4: Run and check the content",
        "手順4：実行して内容を確認する",
        "4단계: 실행하고 내용 확인"
      ]
    },
    {
      "type": "code",
      "language": "powershell",
      "label": [
        "Windows PowerShell，在教材資料夾內",
        "Windows PowerShell, in the exercise folder",
        "教材フォルダーの Windows PowerShell",
        "실습 폴더의 Windows PowerShell"
      ],
      "code": "py -3 --version\npy -3 run_summary.py run-01\n$practiceExit = $LASTEXITCODE\n$practiceExit\n"
    },
    {
      "type": "code",
      "language": "sh",
      "label": [
        "macOS／Linux，在教材資料夾內",
        "macOS / Linux, in the exercise folder",
        "教材フォルダーの macOS／Linux",
        "실습 폴더의 macOS / Linux"
      ],
      "code": "python3 --version\npython3 run_summary.py run-01\npractice_exit=$?\nprintf '%s\\n' \"$practice_exit\"\n"
    },
    {
      "type": "p",
      "text": [
        "若 Windows 沒有 py，但 python --version 顯示 Python 3，可把 py -3 換成 python。預期終端機輸出一個物件，revision 為 exec-practice-1，數值為 total 3、completed 1、pending 2，退出 0。開啟 run-01，核對五份檔案。驗證器會拒絕缺欄、多欄、負數、以 true 冒充整數及數學不一致；但 3／2／1 雖然總和正確仍是錯誤內容，因此最後還要和清單核對。結構符合 schema 不等於事實正確。",
        "If Windows lacks py but python --version reports Python 3, replace py -3 with python. Expect one object with revision exec-practice-1 and total 3, completed 1, pending 2, followed by exit 0. Inspect the five files in run-01. The validator rejects missing/extra fields, negatives, booleans masquerading as integers and inconsistent arithmetic. But 3/2/1 has valid arithmetic and is still wrong, so compare with the list. Schema compliance does not prove factual accuracy.",
        "Windows に py がなく python --version が Python 3 なら py -3 を python に替えます。版 exec-practice-1、total 3、completed 1、pending 2 のオブジェクトと0終了を期待し、run-01 の5ファイルを確認します。検証器は欄の不足・追加、負数、整数の代わりの true、計算不一致を拒否します。ただし3／2／1は合計が合っても内容が誤りなので、一覧との照合も必要です。schema 適合だけで事実の正しさは証明できません。",
        "Windows에 py가 없고 python --version이 Python 3이면 py -3를 python으로 바꿉니다. 버전 exec-practice-1, total 3, completed 1, pending 2인 객체와 종료 0을 기대하고 run-01의 다섯 파일을 확인하세요. 검증기는 필드 누락·추가, 음수, 정수 대신 true, 산술 불일치를 거부합니다. 그러나 3/2/1은 합이 맞아도 틀린 내용이므로 목록과 비교해야 합니다. schema 준수만으로 사실 정확성을 증명하지 못합니다."
      ]
    },
    {
      "type": "h2",
      "text": [
        "步驟 5：三個不花模型額度的失敗練習",
        "Step 5: Three failure drills without model usage",
        "手順5：モデル利用のない3つの失敗実習",
        "5단계: 모델 사용량 없이 실패 세 가지 실습"
      ]
    },
    {
      "type": "p",
      "text": [
        "用檔案管理員複製整個 run-01 成 run-bad-fields，只在副本 final.json 刪除 pending 欄位；執行下方 verify-only。預期 stderr 出現 Not accepted 且退出非 0。接著另複製原始 run-01 成 run-bad-events，把 events.jsonl 任意一個有效物件的最後一個右大括號刪掉；換成該資料夾名稱再驗證，也應失敗。第三個副本 run-bad-status 只把 status.json 的 exit_code 改成 7，即使 final.json 正確仍應拒絕。每個案例都從未改動的原始產物複製，不互相疊加錯誤。",
        "Copy the entire run-01 folder to run-bad-fields in the file manager and remove only pending from the copy's final.json. Run verify-only below; expect Not accepted on stderr and a nonzero exit. Make a separate copy of original run-01 as run-bad-events, remove the final closing brace from one event object, and verify that folder; it must fail too. In a third copy, run-bad-status, change only status.json exit_code to 7: even a correct final.json must be rejected. Start each case from the unchanged original rather than stacking errors.",
        "ファイル管理画面で run-01 全体を run-bad-fields にコピーし、コピー final.json の pending だけを削除します。下記 verify-only は stderr に Not accepted を出し、非0終了するはずです。元の run-01 を別に run-bad-events へコピーし、イベントオブジェクト1つの最後の閉じ波括弧を消してそのフォルダーを検証します。これも失敗します。3つ目の run-bad-status は status.json の exit_code だけ7にし、正しい final.json でも拒否されることを確認します。誤りを重ねず、各例は不変の原本から作ります。",
        "파일 관리 화면에서 run-01 전체를 run-bad-fields로 복사하고 복사본 final.json의 pending만 삭제합니다. 아래 verify-only는 stderr에 Not accepted와 0이 아닌 종료를 보여야 합니다. 원본을 별도로 run-bad-events로 복사해 이벤트 객체 하나의 마지막 닫는 중괄호를 지운 후 해당 폴더를 검증하면 역시 실패해야 합니다. 세 번째 run-bad-status에서는 status.json의 exit_code만 7로 바꿉니다. final.json이 맞아도 거부되어야 합니다. 오류를 누적하지 말고 매번 변경 없는 원본에서 복사하세요."
      ]
    },
    {
      "type": "code",
      "language": "powershell",
      "label": [
        "Windows：只驗證副本，不呼叫 Codex",
        "Windows: validate the copy without calling Codex",
        "Windows：Codex を呼ばずコピーを検証",
        "Windows: Codex 호출 없이 복사본 검증"
      ],
      "code": "py -3 run_summary.py run-bad-fields --verify-only\n$LASTEXITCODE\n"
    },
    {
      "type": "code",
      "language": "sh",
      "label": [
        "macOS／Linux：只驗證副本",
        "macOS / Linux: validate the copy only",
        "macOS／Linux：コピーだけ検証",
        "macOS / Linux: 복사본만 검증"
      ],
      "code": "python3 run_summary.py run-bad-fields --verify-only\necho $?\n"
    },
    {
      "type": "h3",
      "text": [
        "延伸：格式通過，但內容不符",
        "Extension: valid structure, incorrect content",
        "応用：形式は合格、内容は不一致",
        "확장: 형식은 통과하지만 내용은 틀림"
      ]
    },
    {
      "type": "p",
      "text": [
        "再從原始 run-01 複製出未使用的 run-wrong-counts，只把 final.json 換成下方完整物件。保留 status 與 events 原樣，使用相同 verify-only 命令但改為這個資料夾。預期程式退出 0，因為型別、版本和總和都符合；人工比對 tasks.md 則必須拒絕這份答案。它沒有重讀輸入，也沒有核對最終檔與事件中的回答是否相同，不能作為通用事實驗證器。",
        "Copy original run-01 to an unused run-wrong-counts folder and replace only final.json with the object below. Keep status and events; run verify-only with this folder name. Expect exit 0: types, revision and arithmetic satisfy the validator. Comparing tasks.md must still reject the answer. The verifier neither rereads the input nor checks that final.json matches the answer in events; it is not a general factual verifier.",
        "元の run-01 を未使用の run-wrong-counts に複製し、final.json だけ次のオブジェクトへ置き換えます。status と events を保ち、同フォルダーで verify-only を実行します。型・版・合計が適合するので終了 0 でも、tasks.md との照合では拒否します。この検証器は入力を再読せず、最終ファイルとイベント内回答の同一性も照合しないため、一般的な事実検証器ではありません。",
        "원본 run-01을 비어 있는 이름 run-wrong-counts로 복사하고 final.json만 아래 객체로 바꿉니다. status와 events는 유지하고 해당 폴더에 verify-only를 실행하세요. 타입·버전·합계가 맞아 종료 0이지만 tasks.md와 비교하면 답을 거부해야 합니다. 이 검증기는 입력을 다시 읽거나 최종 파일과 이벤트 답의 일치를 확인하지 않으므로 일반적인 사실 검증기가 아닙니다."
      ]
    },
    {
      "type": "code",
      "language": "json",
      "code": "{\n  \"revision\": \"exec-practice-1\",\n  \"total\": 3,\n  \"completed\": 2,\n  \"pending\": 1\n}\n",
      "label": [
        "run-wrong-counts/final.json：刻意錯誤的計數",
        "run-wrong-counts/final.json: deliberately incorrect counts",
        "run-wrong-counts/final.json：意図的に誤った件数",
        "run-wrong-counts/final.json: 의도적으로 틀린 개수"
      ]
    },
    {
      "type": "p",
      "text": [
        "把此例記成「結構通過／內容拒絕」，不改原始成功紀錄，也不自動提交或使用答案。回到未改動 run-01 驗證並人工核對，應仍為 3／1／2。下一篇 CI 會對固定教材加上精確數值檢查，展示如何把這個人工判斷寫成可執行條件。",
        "Record structure passed/content rejected without altering the original result or using this answer downstream. Recheck untouched run-01 and its 3/1/2 counts. The next CI lesson adds exact fixture comparison to make this acceptance condition executable.",
        "「構造合格／内容拒否」と記録し、元の記録や後続処理に反映しません。未変更 run-01 の検証と 3／1／2 を再確認します。次の CI 篇では固定教材の正確な値を比較して、この採用条件を実行可能にします。",
        "구조 통과/내용 거부로 기록하고 원본 기록이나 후속 처리에 적용하지 않습니다. 미변경 run-01과 3/1/2를 다시 확인하세요. 다음 CI 글은 고정 실습값을 정확히 비교해 이 승인 조건을 실행 가능한 검사로 만듭니다."
      ]
    },
    {
      "type": "h2",
      "text": [
        "還原、限制與下一步",
        "Recovery, limits and next steps",
        "復元・制限・次の学習",
        "복원·제한·다음 단계"
      ]
    },
    {
      "type": "p",
      "text": [
        "還原時不用請 Codex 修 JSON；保留失敗副本當證據，再對原始 run-01 執行 verify-only，應重新通過。下一次真正執行用 run-02；重用 run-01 會因目錄已存在而拒絕覆寫。若出現 launch_failed，先在同一終端機查 codex --version；若是 timeout，查看 [自動化恢復](article:codex-automation-recovery) 再決定是否重試。程式只負責這個單次工作，不追蹤整台主機所有子程序，也不提供跨機器的唯一執行鎖。",
        "Do not ask Codex to repair JSON for recovery. Keep the failed copies as evidence, then verify original run-01 again; it should pass. Use run-02 for the next real execution: reusing run-01 refuses to overwrite an existing directory. For launch_failed, check codex --version in the same terminal. For timeout, consult [Automation recovery](article:codex-automation-recovery) before retrying. This wrapper manages one invocation, not every descendant process on the host or a distributed execution lock.",
        "復元のため Codex に JSON 修正を頼みません。失敗コピーを証拠として残し、元の run-01 を再検証すると合格するはずです。次の実行は run-02 とし、既存 run-01 の上書きは拒否されます。launch_failed は同じ端末で codex --version を確認し、timeout は[自動化の復旧](article:codex-automation-recovery)を読んでから再試行を判断します。このラッパーは1回の起動を管理するだけで、ホストの全子孫プロセスや分散実行ロックを管理しません。",
        "복원을 위해 Codex에 JSON 수정을 요청하지 않습니다. 실패 복사본은 증거로 남기고 원본 run-01을 재검증하면 통과해야 합니다. 다음 실제 실행은 run-02를 사용하며 기존 run-01 덮어쓰기는 거부됩니다. launch_failed는 같은 터미널에서 codex --version을 확인하고 timeout은 [자동화 복구](article:codex-automation-recovery)를 읽은 뒤 재시도를 결정합니다. 래퍼는 한 번의 호출만 관리하며 호스트의 모든 자손 프로세스나 분산 실행 잠금은 관리하지 않습니다."
      ]
    },
    {
      "type": "p",
      "text": [
        "CLI 事件可能加入新種類，因此程式保留未知事件，不靠固定的 item 數量判定成功；遇到未完成或含失敗事件則保守拒絕，讓人調查。這裡只驗證一個回合，不直接套用到續接多回合紀錄。Windows、macOS、Linux 都能以相同 Python 原始碼操作；手機不在本機執行此腳本。正式交付前記錄 Python／CLI 版本、輸入版本、檔案及人工計數結果，再進入 [CI 工作流程](article:codex-ci-workflows)。本教材的參考驗證會區分模擬事件測試與真正模型輸出，不把自行生成的示範 JSON 稱為實際 API 回覆。",
        "New CLI event kinds may appear, so the program retains unknown events and does not require a fixed item count. It conservatively rejects incomplete or failed turns for investigation. It validates one turn, not an arbitrary resumed multi-turn transcript. Windows, macOS and Linux use the same Python source; a phone does not run this local script. Record Python/CLI versions, input revision, files and manual counts before moving to [CI workflows](article:codex-ci-workflows). Reference checks distinguish synthetic-event tests from actual model output; authored demonstration JSON is not presented as an API response.",
        "CLI イベントは増える可能性があるため未知種を保存し、固定 item 数で成功を決めません。未完了や失敗を含むターンは調査のため保守的に拒否します。1ターンの検証であり、任意の再開済み複数ターン記録には直接適用しません。Windows・macOS・Linux は同じ Python を使い、携帯でローカルスクリプトは動かしません。Python／CLI 版、入力版、ファイル、手計算を記録して [CI](article:codex-ci-workflows)へ進みます。参考検証は模擬イベントと実際のモデル出力を区別し、教材 JSON を実 API 回答と称しません。",
        "CLI 이벤트 종류가 늘 수 있어 프로그램은 알 수 없는 이벤트를 보존하며 고정 item 수로 성공을 정하지 않습니다. 미완료·실패 회차는 조사하도록 보수적으로 거부합니다. 한 회차를 검증하며 재개된 여러 회차 기록에 그대로 적용하지 않습니다. Windows·macOS·Linux는 같은 Python 소스를 쓰고 휴대전화는 로컬 스크립트를 실행하지 않습니다. Python/CLI 버전·입력 버전·파일·수동 개수를 기록한 뒤 [CI](article:codex-ci-workflows)로 진행하세요. 참고 검증은 합성 이벤트 시험과 실제 모델 출력을 구분하며 교육용 JSON을 실제 API 답변이라 부르지 않습니다."
      ]
    }
  ]
}
