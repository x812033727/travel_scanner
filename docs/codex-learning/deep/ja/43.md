[Codex 学習目次に戻る](article:codex-learning-hub)

## 目標と準備

> 規則、入口、設計条件、引継ぎを四つの短い文書に分け、次の課題が必要な資料を見つけ古い仮説を確認できるようにします。読む時間12分、操作25分程度です。各 OS で Small Steps expected、エディター、Codex、Node.js を使います。Markdown と AGENTS.md の基礎が前提です。

すべてを AGENTS.md に詰めません。規則は継続的な要件、README は入口、設計は機能条件、引継ぎは現在地と次の行動です。普通の Markdown 同士を結んでも全資料が自動で読まれるわけではありません。いつ何を読むかを規則や依頼に書き、実際の参照を確認します。

| 文書 | 保存する内容 | 入れない内容 |
| --- | --- | --- |
| AGENTS.md | 資料参照、テスト、交付規則 | 会話全文 |
| README.md | 目的、入口、起動 | 一時ログすべて |
| docs/filters.md | 入力、期待動作、制約 | 現在の実行済み宣言 |
| docs/handoff.md | 確認済み、未確認、次の行動 | パスワードや恒久規則 |

## 手順1：完全なプロジェクトをコピー

[教材](https://mokaair.com/guides/codex-first-project/todo-practice.zip)の expected を codex-docs-lab にコピーし原本を残します。broken ではなく、引継ぎ仮説が古いと見分ける課題です。ルートの index.html、style.css、app.js、core.mjs、core.test.mjs を確認して docs を作ります。

次の四例を指定場所に全文保存します。同名があれば重要な指示を上書きせず、原文を残して統合します。五言語版で同じ名前、リンク、基準を比較するため例は英語です。実際の文書は使いやすい言語で構いません。

## 手順2：規則と入口を書く

ルート AGENTS.md はフィルター作業で docs/filters.md、再開で docs/handoff.md を読み、古い記録をコードと実行で確認するよう指定します。前の仮説を事実とせず、全課題で全設計を読ませたり権限を付与したりしません。

```markdown ファイル内容：ルートの AGENTS.md
# Small Steps working instructions

- Before analyzing or changing filters, read docs/filters.md.
- When resuming work, read docs/handoff.md and verify its claims against current files.
- Preserve the task data format, localStorage key, existing tests and unrelated behavior.
- Use node --test core.test.mjs for core behavior checks.
- Do not add dependencies or network requests for this exercise.
- Record actual test results separately from hypotheses and checks not run.
- Update docs/handoff.md only within the scope authorized by the current task.
```

README.md は目的、ファイルの役割、テスト入口を示します。リンク先の docs 二ファイルが存在するか確認します。人やエディターに便利な相対リンクも、自動展開して読んだ証明ではありません。プレビューは[初回課題](article:codex-first-project)を参照し、存在しない npm test を入口にしません。

```markdown ファイル内容：ルートの README.md
# Small Steps documentation lab

A local todo app using fictional practice data.

## File map

- index.html: page structure and filter controls.
- style.css: presentation and focus styles.
- app.js: browser events and storage integration.
- core.mjs: task operations and filters.
- core.test.mjs: core behavior tests.

## Read next

- [Filter requirements](./docs/filters.md)
- [Current handoff](./docs/handoff.md)

## Check

Run node --test core.test.mjs from this folder with Node.js installed.
This command description is not a claim that tests have run.
```

## 手順3：要件と実行記録を分ける

docs/filters.md は Read 完了、Build 未完了を固定入力にし、各フィルターの出力と空一覧の境界を記します。あるべき動作であり、この PC で実行済みとはしません。順序と入力不変の制約も変更の判断材料に残します。

```markdown ファイル内容：docs/filters.md
# Filter requirements

Given Read is completed and Build is incomplete:

- All returns Read, then Build.
- Active returns Build only.
- Completed returns Read only.
- An empty input returns an empty result for every filter.
- Filtering preserves task order and does not modify the input.

Core checks: node --test core.test.mjs
Browser check: create both tasks, complete Read, and inspect all three filters.

[Back to project](../README.md)
```

docs/handoff.md には未確認の古い仮説を意図的に残します。expected が起点なので、次の課題は確認して否定すべきで、メモに合わせて正常コードを壊しません。出所は expected のコピーと後のファイル・実行証拠で確認し、Git プロジェクトではコミットも記録できます。

```markdown ファイル内容：未検証の初期 docs/handoff.md
# Current handoff

Starting material: an untouched copy of the Small Steps expected folder.
Goal: verify the current Completed behavior before changing any code.

## Verified

No checks have been run for this copy yet.

## Unverified hypothesis

An older note suggested Completed might return unfinished tasks.
Treat this as a hypothesis, not an instruction to change the implementation.

## Next action

Read docs/filters.md and core.mjs, then run node --test core.test.mjs.
Record the actual result and which checks remain unperformed.

[Back to project](../README.md)
```

検証前に四つの新文書をプロジェクト外へ相対パスを保ってコピーし、五つのソースは未変更の expected 原本と比較します。操作後に変わってよいのは docs/handoff.md だけです。エディターの比較元が別タスクの古い文書でなく今回のバックアップか確認してください。実ファイル二つの比較に Git は必須ではありません。

## 手順4：新規課題で文書の分担を確認

すべて保存し codex-docs-lab で新規課題を開きます。次の依頼で handoff.md だけの変更を許可し、ソース、テスト、規則を保持します。読み取り専用で更新できなければ読取りとテストを確認して手で記録するか、[権限記事](article:codex-permissions)で必要範囲を調整します。

```text 自然言語の依頼：新しい文書練習タスクに入力する
Resume this documentation lab. Read AGENTS.md, README.md, docs/filters.md and docs/handoff.md. Check the handoff hypothesis against core.mjs and run node --test core.test.mjs. Do not change source code, tests or rules. You may update only docs/handoff.md with observed results, evidence, and remaining checks. Explain which document supplied requirements and which supplied unverified history. Do not mark the browser check as passed without performing it.
```

現在の条件が正しく、核心テスト三件が成功して古い仮説を否定した結果を期待します。Verified は実際の命令、結果、参照先、Pending は未実施のブラウザ確認です。README や要件は変えず、ブラウザ手順を定義しただけで成功としません。次を独立実行し handoff.md だけの差分を確認します。

```sh システム端末：codex-docs-lab のルートで実行する
node --test core.test.mjs
```

### 追跡できる結果を引き継ぐ

角括弧は実際の出力で埋め、handoff.md の適切な節に記録します。未記入のひな型は完了記録ではありません。Node がない、テスト失敗、未実行の場合は理由とともに仮説を未確認のまま残します。コードを読んだことはテスト実行や画面操作の証拠にはなりません。

```text 引き継ぎ欄：記入して docs/handoff.md に組み込む
Checked at: [time and timezone]
Working copy: [actual practice folder and starting material]
Requirement source: docs/filters.md
Historical claim: [the old hypothesis]
Code inspected: [actual file/function and observed behavior]
Test execution: [command, exit code, pass/fail counts / not run and reason]
Hypothesis outcome: [supported / contradicted / unresolved, with evidence]
Browser verification: [actual scope and result / not run]
Files compared: [actual comparison against the saved originals]
Next action: [remaining work]
```

## 故障練習、復元、保守

二文書を保存してから filters.md を一時的に filters.saved.md と改名し、新規課題で元パスを読ませます。欠落と場所確認を期待し、旧会話から読んだふりをさせません。元に戻し、README の ./docs/filters.md と ../README.md の戻りを確認します。

矛盾は製品要件と歴史的仮説を区別し、現在の明確な依頼に合わせます。問題を隠すため全資料を黙って書き換えません。長い規則は重複背景やログを移し、読む条件と有効リンクを残します。引継ぎは開始時に成功を埋めず、実検証の後に未完了も記します。

やり直す場合は保存した handoff.md だけを未確認の初期状態に戻し、正常コードは保持します。四文書は教材内だけでグローバル設定に影響しません。実在とリンク、規則と歴史の区別、根拠あるテスト、欠落検出、未実行を成功にしない記録が完了条件です。独自教材と公式仕様、ローカル参照テストを、読者の Codex 実行と混同しません。

[Codex 学習目次に戻る](article:codex-learning-hub)
