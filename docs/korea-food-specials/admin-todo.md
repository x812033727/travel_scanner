# 上線後的後台待辦（站主操作，或站主同意後我代辦）

這些都**不是**這一批 PR 的內容——它們動的是正式站資料庫，不是 repo。文章本身寫對就好，這裡逐項記下來，免得散在各篇規格裡漏掉。每一項都寫了「為什麼」與「怎麼確認做完了」。

## A. 一定要做（不做會有看得見的毛病）

| # | 事項 | 為什麼 | 怎麼確認 |
| --- | --- | --- | --- |
| A1 | 料理「刀削手擀麵」改名成**刀切麵** | 站主 2026-09-20 已同意。官方 KTO 繁中料理辭典的名字就是刀切麵；「刀削手擀麵」在官方繁中站 0 筆。文章與子主題 `kr-kalguksu` 都已經是刀切麵，只剩料理卡沒改 | `GET /api/travel/foods?country_code=KR` 裡 `kalguksu` 的 zh-TW 名稱是刀切麵 |
| A2 | 後台建立的那幾道料理**各存一次**（不改內容，只是重存） | 它們的 zh-TW 搜尋文字沒建好：現在 `q=一隻雞` 回 0 筆、`q=닭한마리` 回 1 筆。讀者用中文搜就找不到 | `/api/travel/foods/merchants?q=一隻雞` 回非空；`q=醬蟹`、`q=韓式豬腳`、`q=刀切麵`、`q=雪濃湯` 同樣 |
| A3 | PR-B 新增的代表店補 **Naver 地點頁與座標** | 它們落地為 pending／inactive／unverified，不會公開。補了才會出現在店家目錄 | 後台座標佇列該筆變成已驗證；公開 API 查得到 |

## B. 資料更正（有官方依據，但要人工進後台改）

| # | 事項 | 依據 | 備註 |
| --- | --- | --- | --- |
| B1 | `seoul-myeongdong-kyoja` 的地址：目錄是 `퇴계로 129`，**兩個官方來源都寫 `명동10길 29`**（Visit Seoul 店家頁 `KOP008570`，2026-04-30 更新；서울미래유산登錄頁） | 目錄那筆的來源只是 `data.seoul.go.kr` 的座標資料集，依本批來源規則本來就不是可用依據 | 刀切麵篇的文章已經寫 `명동10길 29`。**不要**推測 `퇴계로 129` 是哪一家分店 |
| B2 | `daegu-2-day-itinerary` 寫「막창구이：烤牛的第四個胃 홍창」 | R-daegu 查證：官方來源今天不是這樣寫（大邱豬、牛都有，沒有來源用 홍창） | 這是 repo 內容包，走反向連結票一起改，不是後台 |

## C. 分類補齊（讓既有的公開店家連上新料理）

| # | 事項 | 為什麼 |
| --- | --- | --- |
| C1 | 料理 `jokbal` 的適用城市加 `busan` | 釜山有豬腳店，但這一批沒寫釜山豬腳篇（只有 2 家 A 級店，未達下限） |
| C2 | 大邱四家燉排骨店連上 `kr-jjim-galbi` | 那四家在 `trend_merchants.json` 裡、已經公開；種子不能用它們當代表店（會要改兩個測試），所以改在後台連 |
| C3 | 明洞餃子等已公開的店連上後台建立的料理（例：`kalguksu`） | 料理卡現在顯示「目前沒有已驗證店家」 |
| C6 | **首爾目錄裡沒有任何豬腳店**，`jokbal` 也沒有進 `DISH_CATEGORIES` | 豬腳篇的文末目錄連結因此只能寫「其他家常料理店家」，不能承諾有豬腳店。要補的話是把那五家與 `jokbal` 補進目錄 |
| C5 | **大邱燉排骨店的自訂中文店名與官方不符**：目錄寫「珊瑚燉排骨食堂」，官方繁中頁寫「Sanho燉排骨」；「洛榮」「笑瞇瞇」官方沒有中文名（是目錄自己取的） | 文章照官方寫，目錄那幾筆要不要對齊由站主決定 |
| C4 | **하동관的招牌菜標成泡菜**（公開目錄的 `signature_dishes` 是 `kr-kimchi`） | 官方頁寫它只賣곰탕與수육兩樣。PR #581 認養這筆時加了 `kr-gomtang`，但舊的泡菜標籤還在 |

## E. 正式站資料品質（研究階段撞到的，不是這一批造成的，但要記著）

這兩件都是咖啡店研究代理在查官方來源時撞到的，**要各開一張票**，不在這一批的 PR 裡處理。

| # | 事項 | 證據 | 影響 |
| --- | --- | --- | --- |
| E1 | **커피상점 이심的地址在兩份資料差一個行政區**：`trend_merchants.json` 與 2019 年觀光公社文章都寫 `마포구 동교로46길 42`（延南洞），但正式站公開目錄寫 `서대문구 연희로15길 66`（延禧洞，2026-09-09 驗證） | 兩個地址相差一個區，不可能都對 | 延南洞・弘大篇**因此沒有收錄這家店**。要人工核實哪個是對的（或它其實搬過家） |
| E2 | **맥심플랜트已經公開在正式站，但入選依據只有 B 級**（品牌官網＋座標資料），沒有任何 A 級來源 | 觀光官方網站上找不到任何一頁描述這家店 | 不是這一批造成的。若站方要維持「公開店家都有可查依據」，這筆要補依據或下架 |

## D. 之後再說（不急，記著就好）

- 這一批每道料理的**第二個城市**（豬肉湯飯、韓式豬腳、冷麵已有兩城市；其餘只有一篇）列為下一批優先。單篇的料理 hub 也會被索引，不影響上線。
- 定期複查票：`kfood-closure-sweep-2026-12`（每季查歇業搬遷）、`kfood-summer-menus-2027-05`。
  **注意**：原本計畫裡的 `kfood-michelin-2027-edition` 已取消——本批全批不寫米其林（官網對我們每個工具都 403，政府頁轉述又互相打架），沒有東西要複查。
