---
id: 2026-09-19-4-6-11-zh-tw-update
title: 三個新聞索引增補 4.6 的 11 篇（只有 zh-TW，update_index.py 要能限定語系）
status: open
priority: P2
area: docs
owner:
claimed_at:
created_at: 2026-09-19T19:44:36Z
completed_at:
branch:
depends_on:
  - 2026-09-19-news-batch-4-6-news-since
scope:
  - docs/news-2026-batch-4/update_index.py
  - apps/api/app/guides/content/ai-news-2026-january-september-index.json
  - apps/api/app/guides/content/tech-news-2026-index.json
  - apps/api/app/guides/content/crypto-news-2026-index.json
---

# 三個新聞索引增補 4.6 的 11 篇（只有 zh-TW，update_index.py 要能限定語系）

## Why

批次 4.6（`2026-09-19-news-batch-4-6-news-since`）的 11 篇只有 zh-TW。三個索引文章
（AI／科技／幣圈 2026 索引）是五語系，4.5 用 `docs/news-2026-batch-4/update_index.py`
原地增補連結時是五語一起做：`NEW` 表跨語系套用（連結文字抄各語系的 title）、`CITED`
把每篇第一個來源加進索引的 sources（上限 20，AI 索引 4.5 後已 19 條）。這兩件事對
zh-TW-only 的批次都不成立，所以 4.6 出 PR 時沒有動索引；11 篇靠 sitemap、Atom feed、
垂直列表頁與 `related` 被發現，索引頁的站內連結留給這張票。

## Definition of done

- [ ] `update_index.py` 支援只在指定語系插入連結（例如 `--locale zh-TW`），其他語系的索引一字不動，
      而且對已插入過的連結第二次執行要拒絕（沿用「每個替換只能命中一次」的做法）。
- [ ] `CITED` 對這批不套用（AI 索引會超過 20 條來源上限）；寫清楚為什麼。
- [ ] 三個索引 zh-TW 各加上 4.6 的連結：AI 4 篇接在 `ai-news-google-cc-family-agent-20260918` 之後（按月份清單）；
      科技 4 篇進各自的主題群（npm→平台與開發者、CISA→資安／法規、Windows→平台、iPhone Duo→硬體或開發者）；
      幣圈 3 篇進各自的地區群（OCC→美國、EBA→歐盟、SEC→美國），連結文字逐字抄各內容包當時的 zh-TW `title`
      （npm 那篇的標題在查核後已改，勿用研究紀錄舊標題）。
- [ ] 索引的 `check_article.py` 不適用；跑 `pack_cli lint --kind life --slug <index>` ×3 與內容測試。
- [ ] 部署後 `guides-import --slug` 三個索引（動作是 update），確認 zh-TW 索引頁出現 11 個連結、其他語系頁不變。

## Steps

- [ ] 讀 `update_index.py` 的 `NEW`／`INSERT`／`CITED` 與 `main()`；決定用旗標或表格內的語系鍵限定。
- [ ] 加 4.6 的表；乾跑看 diff；套用；lint；PR。

## How to verify

`npm run check:tasks`；`cd apps/api && uv run python -m app.guides.pack_cli lint --kind life --slug ai-news-2026-january-september-index`（另兩個索引同）；
部署後三個索引頁 zh-TW 各多出 4／4／3 個 `article` inline，en／ja／ko／zh-CN 頁的區塊數不變。

## Notes

- 4.6 的 slug 與 display_order：AI 167–170（accenture-evaluation、openai-australia-youth-safety、gemini-notebook-study-tools、kimi-k3-bedrock）、
  科技 317–320（npm-stage-only-tokens、cisa-kev-linux-kernel、windows-cloud-rebuild、iphone-duo-dev-resources）、
  幣圈 214–216（occ-three-trust-charters、eba-third-party-risk、sec-crypto-fraud-patterns），都以 `-20260918` 結尾。
- 4.5 的 HANDOVER 第 162 行說明為什麼不重跑 `build_*_index.py`（會整份重寫 zh-TW、蓋掉 relink 與譯文）。
